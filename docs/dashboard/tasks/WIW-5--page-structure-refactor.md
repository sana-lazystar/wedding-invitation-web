---
id: WIW-5
type: task
status: in-progress
created: 2026-09-09 01:23:31
updated: 2026-09-09 01:47:53
completed:
epic:
jira:
repos: [wedding-invitation-web]
design-refs: [docs/artifacts/index.html, docs/artifacts/README.md, docs/dashboard/discussions/2026-09-06--design-mockups.md]
code-refs: []
related: [WIW-4]
---

# WIW-5 — 한 파일에 몰린 시안 적용 코드를 구조로 나누기

## 목적

시안을 즉시 반영하려고 `src/app/page.tsx` 한 파일에 장면 마크업 · 효과 · 자료를 모두 넣었습니다(WIW-4, 1175줄). 시안이 확정됐으니 읽고 고칠 수 있는 구조로 나눕니다. 화면과 동작은 한 군데도 바뀌지 않아야 합니다.

## 작업내용

- 장면을 구획(와이어프레임 쪽)마다 컴포넌트 파일로 나눕니다. 마크업은 조립본 `docs/artifacts/index.html`과 1:1로 두어 조립본이 바뀌면 그 파일만 고치면 되게 합니다.
- 효과(화면 높이 고정 · 진입 장면 · 쪽지 나타나기 · 덮개 · 카카오맵 · 마지막 장면 · 배경 음악 · 떠 있는 메뉴 · 알림)를 훅 파일로 나눕니다. 실행 순서는 지금과 같게 둡니다.
- 자료(성함 · 계좌 · 예식장 · 제목 · 설명 · OG 이미지)를 `src/content`로, 도구(클립보드 · 소리 페이드 · 카카오 SDK)를 `src/lib`으로 뺍니다. `layout.tsx`와 공유해 제목 · 설명이 한 곳에만 있게 합니다.
- 참조되지 않는 파일을 지웁니다(윈도 타일 설정 · 쓰지 않는 스크립트 둘 · 쓰지 않는 에셋 셋). 레포 첫 문서(`README.md`)의 스캐폴드 문구를 이 프로젝트 안내로 바꿉니다.
- 스코프 경계. CSS는 손대지 않습니다(`globals.css`는 조립본 산출물이고 정본은 조립본입니다). 조립본의 마크업 · 스크립트도 그대로 둡니다. 기능 추가 · 문구 변경은 하지 않습니다. 사진 파이프라인(`docs/scripts/images.mjs`)은 여전히 정식 이식 몫입니다.

## 주의할 점

효과 실행 순서가 화면에 영향을 줍니다. 화면 높이 고정(`--vh-fixed`)이 먼저 서야 커버와 마지막 장면의 치수 계산이 지금 값과 같습니다. 자식 컴포넌트의 효과가 부모보다 먼저 도는 것을 감안해 순서에 얽힌 훅은 페이지가 쥡니다.

리팩터링 전후를 기계로 대조합니다. 같은 조건(WebKit 390×844)에서 DOM · 구획 높이 · 계산된 CSS 변수 · 캡처 · 상호작용 상태를 모아 비교하는 탐침을 먼저 돌려 기준을 잡고, 끝난 뒤 같은 탐침으로 대조합니다.

## 완료 기준

- [x] 탐침 대조에서 DOM 문자열 동일 · 캡처 열두 장 동일 · 진입/마지막 장면 계산값 동일 · 상호작용(뷰어 · 메뉴 · 공유 페이로드 · 복사 · 지도 지연 로딩 · 음악) 동일
- [x] 대상 레포 게이트 통과 (`tsc --noEmit` · `eslint` · `next build`)
- [x] 구현 기록 기입 (브랜치 · 커밋)

단독 task라 PR 초안까지가 완주입니다.

## 구현 기록

2026-09-09 01:47:53 · feature/WIW-5-page-structure-refactor · 1403ea6 구조 분해 · 1059395 미사용 파일 · 첫 문서 정리. 작업 문서는 develop 217c045.

대조 결과(프로덕션 빌드, WebKit 390×844). 리팩터링 전후로 `next build` 결과를 각각 띄워 같은 탐침을 돌렸습니다.

| 잰 것 | 결과 |
| --- | --- |
| DOM 문자열(body 전체) | 동일 |
| 화면 캡처 21장 | 19장 픽셀 동일. 마지막 장면 둘은 음표 애니메이션 위치만 다름(같은 코드로 두 번 재도 다른 자리) |
| 진입 장면 계산값 · 구획 높이 · 쪽지 나타남 수 | 동일 |
| 뷰어 · 메뉴 · 공유 페이로드 · 복사 알림 · 지도 지연 로딩 · 음악 상태 | 동일 |
| 브라우저 오류 | 전후 같음(로컬 프로덕션 서버는 카카오맵 도메인 미등록이라 401 하나) |

## PR 초안

제목: `refactor: 청첩장 코드를 장면 · 훅 · 자료 · 도구로 나눔 (WIW-5)`

본문 요지.

- 시안을 즉시 반영하려고 한 파일에 몰아넣었던 `src/app/page.tsx`(1175줄)를 조립만 하는 88줄로 줄이고, 장면 15개 · 덮개 2개 · 늘 떠 있는 것 5개 · 훅 9개 · 도구 3개 · 자료 2개로 나눴습니다.
- 장면 파일은 조립본 `docs/artifacts/index.html`의 한 구획과 1:1입니다. 조립본이 바뀌면 그 파일만 고칩니다.
- 제목 · 설명 · 예식장 · OG 그림 값을 `src/content/site.ts` 하나로 모아 `layout.tsx`의 메타와 카카오톡 공유 메시지가 같은 값을 씁니다.
- 참조되지 않는 파일 아홉을 지우고 레포 첫 문서를 이 프로젝트 안내로 바꿨습니다.
- 화면 · 동작은 바뀌지 않았습니다(위 대조 표). 같은 브랜치에 은행명 표기 수정(WIW-4, 디자인 논의 T142)이 한 커밋 더 있습니다.

머지: 이산하가 `feature/WIW-5-page-structure-refactor` → `develop` PR로 합니다.

