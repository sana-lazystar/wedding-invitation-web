---
type: discussion
status: in-progress
created: 2026-09-06 18:25:07
updated: 2026-09-07 00:33:39
completed:
participants: 이산하, Claude
related: []
jira:
---

# 디자인 시안 — 와이어프레임에서 화면으로

## 발단

스택·인프라 논의가 끝나고 코드 착수 전 단계입니다. 이산하가 와이어프레임(`docs/design/모바일청첩장_와이어프레임.pdf`, 14블록)과 커버용 일러스트 2장(`docs/design/scene1/`)을 준비했고, 사진 원본 20장이 `docs/gallery/`에 있습니다. 이산하는 Claude Design으로 시안을 만들고, Claude는 시안을 레포에 정리하고 Artifact로 발행해 폰에서 확인할 수 있게 하는 흐름을 원합니다. 이 논의는 그 흐름과 저장 구조를 정합니다. 와이어프레임이 "청첩장에 무엇이 들어가는가"를 이미 답하므로, 요구 정의 논의를 따로 열지 않고 이 논의에서 요구 원장을 함께 채웁니다.

## 로그

### 2026-09-06 18:25:07 이전 · 이산하 (T1)

- (compact 직전 턴) 와이어프레임과 재료가 있으니 실제 디자인을 HTML로 만들어 가는 과정을 거치자. 시안은 Claude가 아니라 `design` 명령으로 Claude Design을 써서 만든다. 재료는 아직 다 전달하지 않았다.
- 디자인 시안을 뽑자. 시안은 Artifact로 확인할 수 있게 하고, URL별로 HTML 파일이 나오는 구조가 좋겠다.
- `docs/ontology/ia/`에 URL별로 두기로 한 대로 하면 될 것 같은데, 어떻게 하는 것이 좋은가.
- 모바일 청첩장은 관례상 URL 하나에 스크롤로 끝낸다.

### 2026-09-06 18:28:14 · Claude (T2)

- 읽은 것. 와이어프레임 16쪽 전부, 커버 일러스트 2장(860×1428 신랑신부 컷아웃 PNG, 860×1829 커티지홀 아치 배경), `docs/ontology/README.md` §서랍 정의의 `ia/` 행, README §논의의 `artifacts/` 점검 규칙, DesignSync 도구 명세.
- 와이어프레임 요지. 단일 페이지 14블록 약 14.8화면. 커버·핵심 정보·마지막은 1화면 고정이고 사이는 자유 흐름이다. 하단 고정 바에 오시는 길·연락처 버튼이 상시 노출된다. 3~8번 블록만 캐릭터(토끼 = 신부, 메모지 왼쪽 · 수달 = 신랑, 오른쪽)가 등장하고, 이야기 구간은 요체·초대 이후는 격식체다. 레이아웃 규칙은 배경 위 종이, 종이 위 사진·글, 그 위 오브제(종이 밖으로 걸침), 본문 한 줄 약 22자·줄 간격 1.7배, 메모지 안쪽 여백 좌우 16·위아래 20이다. 블록은 1 커버, 2 핵심 정보(아치), 3 인사, 4 Part 1 신랑, 5 Part 1 신부, 6 Part 2 신랑, 7 Part 2 신부, 8 Part 3, 9 초대(편지지), 10 만화 + 갤러리(9장 + "+31" 타일, 전체화면 뷰어), 11 오시는 길(약도·주차·지하철·버스), 12 하객 안내, 13 마음 전하는 곳(계좌 6개, 행 전체 복사), 14 마지막(커버 아치 축소)이다.
- 판단 1, 단위. IA 단위는 URL이 아니라 블록이다. URL은 `/` 하나이고 갤러리 뷰어도 같은 URL 위의 오버레이다. "URL 1개 = 파일 1개" 원칙은 유지하되 지금은 파일이 하나씩이다. 블록마다 앵커 id를 두어 명세·시안·코드가 같은 이름으로 블록을 부른다.
- 판단 2, 세 자리. ① 명세는 `docs/ontology/ia/`에 둔다(텍스트 정본, `/` = `index.md`, 라우트 표는 `README.md`). 와이어프레임 PDF는 git 제외 폴더에 있으니 `index.md`가 그 git 정본이 된다. ② HTML 시안은 `docs/artifacts/`에 URL 경로를 그대로 미러한다(`/` = `index.html`). ontology는 살아있는 정본 텍스트만 두는 서랍이고 HTML 시안은 코드가 생기면 스냅샷이 되므로 ontology 밖이 맞다. README §논의가 `artifacts/`를 "살아있는 데모"로 이미 예약했고 `check-refs.mjs`가 `docs/artifacts/README.md`를 스캔 대상에 넣어 두었다. ③ 라운드 비교가 필요한 시점의 사본은 `docs/ontology/references/YYYY-MM-DD--design-round-N.html`로 동결한다.
- 판단 3, 확인 방식. Artifact 샌드박스는 외부 이미지·CSS 로딩을 막으므로 발행본은 이미지를 data URI로 인라인해야 한다. 원본 HTML(`docs/artifacts/index.html`)은 상대 경로로 이미지를 참조하고, 발행 직전 스크립트(`docs/scripts/inline-artifact.mjs`, 스택 결정 10)가 이미지를 웹 크기로 줄여 인라인한 사본을 스크래치패드에 만든다. 같은 파일을 같은 Artifact에 재발행하니 링크는 하나로 고정되고 버전 라벨로 라운드를 구분한다. 발행 한도는 16MB다.
- 판단 4, 흐름. 한 라운드는 ① 이산하가 Claude Design에서 블록 몇 개를 만들어 HTML을 내보낸다 ② Claude가 `docs/artifacts/index.html`에 합치고 인라인 사본을 Artifact로 재발행해 링크를 준다 ③ 이산하가 폰에서 보고 피드백한다 ④ Claude가 피드백과 결정(키 컬러·폰트·질감 등)을 이 논의록에 적고 `ia/index.md`를 갱신한다. DesignSync 도구는 Claude Design의 "디자인 시스템" 타입 프로젝트만 읽고 쓴다. 이산하의 프로젝트가 그 타입이면 Claude가 파일을 직접 끌어오고, 아니면 내보낸 파일을 이산하가 `docs/artifacts/`에 넣는다.
- 판단 5, 개인정보. 와이어프레임 13번의 계좌 6개와 양가 부모님 성함은 사이트에 실리므로 스택 결정 14의 허용 범위 안이다. 그래도 시안·명세에는 와이어프레임처럼 자리표시(`000-0000-0000`, `이OO`)만 두고, 실값을 넣는 시점·방식은 따로 결정한다(Q10).
- 와이어프레임 16쪽 "남은 작업"에서 원장으로 옮길 것. 요구 후보 5건(검색엔진 차단 noindex · 카카오톡 공유 미리보기 이미지·캐시 사전 테스트 · 라이트 모드 고정 · 어른 하객 가독성(얇은 폰트·옅은 회색 금지) · 이미지 용량 최적화)은 구조 확정 뒤 REQ로 등재한다. 미확정 6건은 지금 원장에 올렸다. Q8 키 컬러·포인트 컬러·종이 질감 강도, Q9 캐릭터 표정 세트, Q10 계좌·성함 실값 반영 시점·방식, U-5 신부대기실 정리 시각, U-6 하객 대기 공간·식사 방식, U-7 예식 당일 연락처 담당.
- 다음. 결정 1~6이 확정되면 `docs/ontology/ia/README.md`·`index.md`(와이어프레임 전사 + 앵커 id), `docs/artifacts/README.md`, 인라인 스크립트를 만들고 REQ 5건을 등재한다. 시안 HTML 첫 파일은 Claude Design 산출물이 오면 만든다.

### 2026-09-06 18:53:29 이전 · 이산하 (T3)

- 와이어프레임 PDF는 쪽마다 한 장으로 그렸지만 만들 것은 원페이지 스크롤이다. 그 점을 감안해 PDF 한 쪽씩 논의해 가자.
- "이산하가 Claude Design에서 블록을 만들어 HTML을 내보낸다"는 아니다. Claude가 이산하와 논의하며 한 쪽씩 PDF를 확인하고, 그것을 토대로 `docs/artifacts/index.html`을 갱신하는 편이 낫지 않은가. 이상한 부분이 있으면 말해 달라.
- "이산하가 Claude Design을 활용한다"도 아니다. Claude가 `/design` 명령으로 Claude Design을 동작시키는 것이 맞다.

### 2026-09-06 18:53:29 이전 · 이산하 (T4)

- 일단 Scene1부터 만들자. Scene1은 와이어프레임 2쪽이다. 위에 WEDDING INVITATION 문구, 아래에 두 사람 이름, 가운데 "팝업북 일러스트" 영역에는 `scene1--wedding-hall.PNG`를 쓰고 그 이미지 가운데에 `scene1--married-couple.PNG`를 작게 올린다. 이해했으면 design 스킬로 만들어 보자.
- 전체를 한 번에 만들지 말고, PDF 한 장 한 장을 이야기해 가며 아래에 이어 붙이는 형태로 만들고 마지막에 다듬자.

### 2026-09-06 18:53:29 · Claude (T4)

- `/design`의 정체. Claude Code에 번들된 Claude Design 미리보기다. Claude가 아트보드(`*.dc.html`, 거의 평문 HTML)를 쓰면 캔버스 편집기와 함께 Artifact로 발행되고, 이산하는 브라우저와 폰에서 열어 보고 글자·간격을 직접 고쳐 저장(Save)하거나 PNG·PDF로 내보낼 수 있다. 편집기는 캔버스마다 박혀 있어 claude.ai/design과 같은 수준은 아니다. 이산하의 말이 맞아 결정 5를 폐기하고 결정 7로 바꿨다.
- 만든 것. 와이어프레임 2쪽(Scene1 커버)만 아트보드 하나(390×844)로 만들어 캔버스를 발행했다. 링크 https://claude.ai/code/artifact/4d3deaa1-1a8a-4818-a815-792618cfea36. 구성은 위 WEDDING INVITATION과 "우리의 삶을 함께 써 주신 당신께", 가운데 302×470 둥근 사각 창에 커티지홀 사진, 그 안 통로 위에 두 사람 컷아웃을 작게(높이 200) 세우고 바닥 그림자를 깔았다. 아래 "이산하 / 그리고 / 송시야", 맨 아래 스크롤 유도 화살과 고정 바(오시는 길·연락처). 가정: 바탕 종이빛 아이보리 `#F3ECDF`, 종이 `#FBF7EF`, 잉크 `#2F2A25`, 선·포인트는 홀의 오크색 `#B98E5E`. 글꼴은 고운바탕(이름·문장) + 고운돋움(안내) + Cormorant Garamond(영문 표제). 키 컬러(Q8)는 이 쪽을 보며 정한다.
- 파일 자리. 캔버스 작업 파일은 `docs/artifacts/canvas/Main.dc.html`·`canvas.json`(커밋). 축소 이미지 `hall.jpg`(366×780, 59KB)·`couple.png`(240×400, 150KB)는 `docs/design/canvas/`(git 제외). 조립된 발행 파일은 스크래치패드에만 있다. 방향 대안 2종(저녁 조명·메모지와 만화)을 먼저 그렸다가 T4 지시에 따라 캔버스에서 뺐고 스크래치패드에 남겼다.
- 이상한 부분 다섯. ① 일정. 예식이 2026-10-09이고 오늘이 09-06이다. 발송을 예식 3~4주 전으로 잡으면 9월 11~18일인데, 14쪽 논의·코드 이식·카카오톡 미리보기 테스트가 남았다. 발송 목표일을 U-8로 올렸고 위험 R3로 적었다. 대응 제안은 캐릭터가 필요 없는 블록(1·2·9~14)을 먼저 완성해 발송 가능한 판을 만들고, 이야기 블록(3~8)은 캐릭터가 준비되는 대로 더하는 것이다. ② 재료. 토끼·수달 캐릭터 그림, 어릴 적 사진 2장, 웨딩 사진 2장, 함께 있는 컷, 만화 4컷, 약도가 아직 없다. 출처를 Q11로 올렸다. ③ 한 화면 고정과 하단 고정 바는 정적 아트보드에서 흉내만 낼 수 있다. 진짜 스크롤 감은 마지막 다듬기 단계의 조립본 `index.html`에서 폰으로 확인한다(결정 9). ④ 이산하가 캔버스에서 직접 고쳐 저장하면 Claude의 작업 파일과 어긋난다. 다음 작업 전에 저장본을 읽어 와 작업 파일에 반영하는 것을 규칙으로 둔다(결정 8). ⑤ 커티지홀 사진은 세로가 긴 비율(1:2.1)이고 와이어프레임의 창은 1:1.4라 위쪽 커튼이 잘린다. 창을 좁혀 사진 전체를 보이는 선택지가 있다.
- 다음. 이산하가 Scene1을 보고 고칠 점을 말하면 반영하고, 확정되면 3쪽(핵심 정보·아치)을 같은 아트보드 아래에 이어 붙인다.

### 2026-09-06 18:55:24 이전 · 이산하 (T5)

- 이런 디자인에는 폰트도 중요한데 아트보드(Artifact)에는 폰트 적용이 안 될 것이다. HTML 파일도 함께 만들고 폰트는 그 파일에 적용해 주면 이산하가 확인하는 식으로 가자. 지금 적용된 폰트는 무엇인가.

### 2026-09-06 18:55:24 · Claude (T5)

- 사실 정정. 캔버스와 Artifact 모두 Google Fonts(`fonts.googleapis.com`)는 허용된 유일한 외부 글꼴 출처라 지금 아트보드의 세 글꼴은 캔버스에서도 적용된다. 안 되는 것은 Google Fonts 밖의 글꼴(Pretendard, 나눔스퀘어 네오, 상업 글꼴 등)과 PNG·PDF 내보내기(대체 글꼴로 찍힘)다. 그런 글꼴은 base64로 심어야 한다.
- 지금 글꼴. 이름·문장 = 고운바탕(Gowun Batang), 안내·"그리고"·고정 바 = 고운돋움(Gowun Dodum), 영문 표제 = Cormorant Garamond. 대체는 Apple SD Gothic Neo · Noto Serif KR · Georgia.
- 조립본을 바로 만들었다. `docs/artifacts/index.html`에 Scene1을 같은 토큰(CSS 변수)으로 옮기고 이미지는 `../design/canvas/`를 상대 경로로 참조한다. 브라우저에서 파일로 열면 어떤 글꼴이든 확인할 수 있어서, 오른쪽 위에 글꼴 확인 패널을 달았다. 본문 후보 7종(고운바탕 · 나눔명조 · 노토 세리프 KR · 함렛 · 송명 · 프리텐다드 · 고운돋움), 안내 후보 5종(고운돋움 · 프리텐다드 · 노토 산스 KR · 나눔고딕 · IBM Plex Sans KR)을 바꿔 볼 수 있다. 패널과 후보 목록은 확정 후 지운다. 결정 9(조립본은 마지막에)는 폐기하고 결정 10으로 바꿨다.
- 여는 법. Finder에서 `docs/artifacts/index.html`을 브라우저로 열거나, 터미널에서 `open docs/artifacts/index.html`. 폰에서 보려면 말해 달라. 이미지를 인라인한 사본을 Artifact로 발행한다(결정 4).

### 2026-09-06 19:02:31 이전 · 이산하 (T6)

- 일정 때문에 기능이나 디자인을 축약하지 말라. 어차피 Claude가 만드는데 상관없다. 고려사항이 아니다.
- 재료는 이산하가 전달한다. 그래서 첫 쪽만 미리 만들라고 한 것이다.
- 진짜 스크롤은 HTML 조립본으로 확인한다. 좋다.
- 캔버스에서 직접 고치지 않을 것이니 걱정하지 말라.
- 지금 만든 상태가 좋다. 고칠 것 넷. ① 한글은 Noto Sans KR이 낫지 않은가 ② WEDDING INVITATION을 좀 더 크게 ③ 이미지의 위치·배치·둥근 외곽은 좋다. 선을 그어 분리하는 것도 좋지만, 그림자는 둔 채 배경색에 자연스럽게 묻히게 해 보라 ④ 이름을 약간만 작게.
- 전체 배경색은 이산하가 지정하지 않았는데 그 색으로 한 이유가 무엇인가.

### 2026-09-06 19:02:31 · Claude (T6)

- 반영 넷. ① 한글 글꼴을 전부 Noto Sans KR로 바꿨다(이름 700, 본문 400, 고정 바 500). 영문 표제는 Cormorant Garamond 그대로. 조립본의 글꼴 패널 기본값도 Noto Sans KR로 맞추고 고운바탕·고운돋움은 후보로 남겼다. ② WEDDING INVITATION 13px → 18px, 자간 0.3em → 0.26em. ③ 종이색 테두리(6px 링)를 없애고 바깥 그림자만 남겼다. 창 안쪽 가장자리에 배경색(`#F3ECDF`)을 반투명으로 번지게 하는 덮개를 얹어 사진이 종이로 스며들게 했다. 둥근 모서리 16px은 유지. ④ 이름 28px → 24px, 자간 0.08em → 0.1em, 줄 사이 6px → 4px.
- 배경색의 근거. 지정이 없어서 커티지홀 사진에서 가져왔다. 사진의 커튼과 조명이 아이보리·크림이라, 그보다 반 톤 어두운 종이빛(`#F3ECDF`)을 깔면 사진이 배경보다 밝게 떠 보이고 두 사람 컷아웃의 흰 외곽선도 살아난다. 와이어프레임의 "종이 질감"과 "라이트 모드 고정"도 순백보다 따뜻한 종이색을 가리킨다고 읽었다. 제안일 뿐이며 키 컬러(Q8)는 열려 있다.
- 원장 정리. 일정 위험 R3은 이산하 판단으로 소멸시켰다. 발송 목표일 U-8은 설계 입력이 아니므로 닫았다. 재료 출처 Q11은 "이산하가 전달"로 닫았다. 결정 1·3·4·8·10의 잠정 항목은 T6에 이의가 없어 확정으로 올렸다. 결정 2(ia 명세)와 6(계좌 자리표시)은 아직 잠정이다.
- 캔버스를 같은 링크에 다시 저장했다(라벨 "Scene1 수정 1"). 조립본 `docs/artifacts/index.html`도 같은 수정을 반영했다. 캔버스·조립본 수정은 커밋 febdb45에 먼저 들어갔고, 이 T6 기록과 원장 갱신은 스크립트 대조 오류로 한 커밋 뒤에 들어간다.

### 2026-09-06 19:05:13 이전 · 이산하 (T7)

- 아래에 있는 "아래로" 표시(화살)는 빼라. 이상하다.
- 오시는 길·연락처를 아래에 고정(position fixed)하려 한 것 같은데 그러지 말라. 오른쪽 아래에 떠 있는 원형 버튼 하나를 두고, 누르면 메뉴(오시는 길 · 연락처 · 마음 전하는 곳)가 뜨고, 고르면 그 섹션으로 스크롤되는 형태가 좋겠다.
- 이름 글씨체는 좀 전에 썼던 것이 낫다. Google Fonts 중에 편지지 글씨체 종류는 무엇이 있는가.

### 2026-09-06 19:05:13 · Claude (T7)

- 반영 셋. ① 스크롤 화살을 뺐다. ② 하단 고정 바를 없애고 오른쪽 아래 52px 원형 버튼(오크색, 줄 세 개 아이콘)을 두었다. 누르면 위로 종이 카드 메뉴(오시는 길 · 연락처 · 마음 전하는 곳, 행 높이 48px)가 열리고 고르면 닫히면서 해당 블록으로 부드럽게 스크롤한다. 바깥을 누르거나 Esc로 닫힌다. 캔버스에는 열린 상태를 보이는 아트보드 `FloatingMenu.dc.html`(390×360)을 옆에 두었다. 조립본에서는 실제로 동작하지만 대상 블록(`#directions`·`#contact`·`#gift`)이 아직 없어 스크롤은 그 쪽을 붙인 뒤 확인한다. ③ 이름 글꼴을 고운바탕으로 되돌렸다. 나머지 한글은 Noto Sans KR 그대로.
- 편지지 느낌의 Google Fonts 한글 글꼴을 조립본 글꼴 패널의 "이름" 목록에 넣었다. 명조·바탕 계열 여섯(고운바탕 · 나눔명조 · 노토 세리프 KR · 함렛 · 송명 · 디필레이아)과 가는 손글씨 하나(그랑디플로라), 손글씨 계열 다섯(나눔손글씨 펜 · 나눔손글씨 붓 · 개구 · 하이멜로디 · 감자꽃). 본문 목록에도 명조 계열을 더했다.

### 2026-09-06 19:06:14 이전 · 이산하 (T8)

- 신랑신부 사진을 오른쪽으로 10px 정도만 옮겨 달라.

### 2026-09-06 19:06:14 · Claude (T8)

- 두 사람 컷아웃과 그 아래 바닥 그림자를 함께 10px 오른쪽으로 옮겼다(가로 기준점 50% → 50% + 10px). 캔버스와 조립본 둘 다 반영하고 캔버스를 같은 링크에 다시 저장했다(라벨 "Scene1 수정 3").

### 2026-09-06 19:06:38 이전 · 이산하 (T9)

- WEDDING INVITATION 문구를 조금만 더 키워 달라.

### 2026-09-06 19:06:38 · Claude (T9)

- 18px → 21px. 자간 0.26em 유지. 캔버스와 조립본 둘 다 반영하고 캔버스를 같은 링크에 다시 저장했다(라벨 "Scene1 수정 4").

### 2026-09-06 19:11:36 이전 · 이산하 (T10)

- 처음 진입할 때 애니메이션으로 편지지가 열리는 식으로 하고, WEDDING INVITATION과 "우리의 삶을 함께 써 주신 당신께"가 화면 중앙에서 스르륵 떠오른(fade up) 뒤 자연스럽게 확장되며 전체 화면이 보이게 할 수 있는가. 편지지 디자인을 한번 시켜 보고 안 되면 이 건은 패스한다.
- 너무 위에서 시작한 것 같으니 WEDDING INVITATION 문구를 포함한 전체 위치를 좀 더 아래로 내려 달라.

### 2026-09-06 19:11:36 · Claude (T10)

- 진입 장면을 조립본에 넣었다(캔버스는 정적이라 동작이 없다). 순서는 ① 반으로 접힌 종이가 화면 가운데 떠오르고 ② 위쪽 반이 위로 펼쳐지며(회전 0.7초) ③ 표제와 문장이 아래에서 위로 스르륵 떠오르고(0.6초, 문장은 0.14초 늦게) ④ 종이가 커지며 화면을 채우는 동안 표제가 커버의 제자리로 옮겨 가고(0.85초) ⑤ 덮개가 걷히며 커버가 나타난다. 전체 약 3.2초. 오른쪽 아래 "건너뛰기"로 즉시 넘길 수 있고, 시스템의 동작 줄이기 설정이 켜져 있으면 장면을 생략한다. 글꼴 패널에 "진입 장면 다시" 버튼을 두어 반복 재생할 수 있다. 표제가 제자리로 가는 이동량은 실행 시점에 커버 표제의 위치를 재서 계산한다.
- 커버 전체를 아래로 내렸다. 위 여백 52px → 80px, 아래 여백 40px → 24px. 캔버스와 조립본 둘 다.
- 폰에서 동작을 볼 수 있도록 결정 4의 인라인 스크립트 `docs/scripts/inline-artifact.mjs`를 만들었다. 조립본이 상대 경로로 참조하는 이미지를 최대 1080px로 줄여(작은 것은 키우지 않음) data URI로 심고, 문서 껍데기를 벗겨 Artifact 형식으로 만든다. 의존성 없이 macOS `sips`만 쓴다. 그 사본을 별도 Artifact "이산하 · 송시야 청첩장 미리보기"로 발행했다. 링크는 대화에 적었고, 이후 조립본이 바뀔 때마다 같은 링크에 다시 발행한다.
- 판정은 이산하 몫이다. 마음에 안 들면 장면만 걷어낸다(마크업 `#intro`와 관련 CSS·JS 한 덩어리).

### 2026-09-06 19:22:14 이전 · 이산하 (T11)

- 진입 장면이 애매하다. WEDDING INVITATION 영역이 처음에 편지지를 넘어서고, 편지지는 그냥 흰 종이 같은 모양새다.
- 가능하면 이것을 Next.js 쪽에 임시 적용해 실제로 로컬에서 실행해 보게 해 달라.
- Vercel이 docs 수정 커밋에도 계속 preview 배포를 돌리는데 제한에 걸릴 것 같다. 필요할 때만 배포돼야 한다. 커밋은 계속해야 한다(기록이 남아야 하므로). Vercel 쪽에서 preview 배포가 안 돌게 하는 것은 이산하가 알아본다.
- 진입 장면은 줄 그은 노트보다 편지봉투가 낫겠다. 봉투가 열리고 WEDDING INVITATION 이하가 적힌 편지지가 나오고 그것이 확대되는 식으로.

### 2026-09-06 19:22:14 · Claude (T11)

- 진입 장면을 봉투로 다시 만들었다(줄 노트 버전은 폐기). 순서는 ① 봉투(뒷면·앞면 V자 주머니·삼각 덮개)가 화면 가운데 조금 아래에 떠오르고 ② 덮개가 위로 젖혀지고(0.7초) ③ 표제가 적힌 편지지(260×180)가 봉투에서 위로 빠져나오며(0.8초) 표제와 문장이 떠오르고 ④ 편지지가 화면을 채우도록 커지는 동안 표제가 커버 제자리로 옮겨 가고(0.85초) ⑤ 덮개가 걷힌다. 약 3.7초. 표제는 편지지 안에 들어가도록 0.72배로 시작해 제 크기로 커진다. 봉투 색은 종이보다 반 톤 어두운 `#E4D6BE` 계열이다. 건너뛰기·다시 보기·동작 줄이기 대응은 그대로다.
- Next.js에 임시 적용했다(WIW-4). `src/app/layout.tsx`(글꼴 3종을 `next/font/google`로, `lang="ko"`, 제목, noindex), `src/app/page.tsx`(클라이언트 컴포넌트로 커버·진입 장면·떠 있는 메뉴), `src/app/globals.css`(조립본 CSS 그대로), `public/scene1/`(축소 이미지 2장 복사). 이산하가 이미 띄워 둔 `next dev`(포트 3000)에 바로 반영됐고 `http://localhost:3000/`이 200으로 커버를 내려 주며 이미지 2장도 200이다. tsc·eslint 통과. 코드는 WIW-4대로 커밋하지 않고 작업 트리에만 둔다.
- 조립본과 Next.js 코드는 CSS를 한 원본에서 생성해 같게 유지한다(글꼴 토큰만 조립본은 Google Fonts 이름, Next.js는 `next/font` 변수). 미리보기 Artifact는 같은 링크에 다시 올렸다(라벨 "진입 장면 3 · 봉투").
- Vercel 배포. 커밋은 계속하고 Vercel이 docs만 바뀐 커밋을 건너뛰게 하는 것이 맞다. 방법은 프로젝트 Settings → Git → Ignored Build Step에 다음 명령을 두는 것이다. `git diff --quiet HEAD^ HEAD -- ':!docs' ':!*.md'` 종료 코드 0(docs와 md 밖에 변경 없음)이면 빌드를 건너뛰고 1이면 빌드한다. 브랜치 자체를 배포에서 빼려면 같은 화면의 Production Branch와 preview 대상 설정을 쓴다. 이산하가 알아보겠다고 했으므로 Claude는 설정을 만지지 않는다.

### 2026-09-06 19:25:47 이전 · 이산하 (T12)

- 지금 수준이면 커밋은 십수 건이 아니라 수백·수천 건이 된다. 어쨌든 됐다.
- 편지지는 지금 마음에 든다. 열리는 부분이 좀 더 입체감이 있었으면 좋겠다.

### 2026-09-06 19:25:47 · Claude (T12)

- 봉투 열림에 입체감을 더했다. ① 덮개를 앞면·뒷면 두 장으로 만들어 젖혀지는 동안 뒷면(안쪽, 더 어두운 종이색)이 드러난다. 회전 중에는 앞면이 점점 그늘지고 뒷면은 밝아지는 명암 애니메이션을 얹었다. ② 봉투 안쪽 윗부분에 그늘 그라데이션을 두어 덮개가 열리면 봉투 속이 보인다. 덮개가 몸통에 드리우던 그늘은 열리면서 걷힌다. ③ 앞 주머니의 V자 가장자리에 1~2px 밝은 사선을 넣어 종이 두께를 냈다. ④ 편지지가 나올 때 살짝 앞으로(translateZ 26px) 나오고 위가 뒤로 5도 기울며, 그림자가 짙어졌다가 확대되면서 평평해진다. ⑤ 원근을 800px에 시점을 위쪽(18%)으로 잡아 덮개의 단축이 더 보이게 했다. 덮개 회전은 0.8초이고 전체는 약 3.85초.
- 조립본·Next.js 코드·미리보기 Artifact 셋 다 반영했다. tsc·eslint 통과, 로컬 200.

### 2026-09-06 19:28:10 이전 · 이산하 (T13)

- 입체감 버전은 오버다. 좀 전의 것이 낫다. 진짜 이상해졌다. 봉투 입체감을 제거하라.
- 차라리 봉투 오른쪽 아래쯤에 필기체로 "Thanks to everyone."이 적혀 있는 것이 좋겠다.

### 2026-09-06 19:28:10 · Claude (T13)

- 입체감(덮개 앞뒷면·명암·안쪽 그늘·편지지 기울기·원근 변경)을 전부 걷어내고 진입 장면 3(커밋 41c16e4의 조립본)으로 되돌렸다. Next.js 코드도 같은 상태로 맞췄다.
- 봉투 앞면 오른쪽 아래에 필기체 "Thanks to everyone."을 넣었다. 글꼴은 Google Fonts의 Dancing Script(500), 17px, 오크 잉크색 `#7E5A33`, 4도 기울임. 편지지가 확대될 때 봉투와 함께 사라진다. Next.js에는 `next/font/google`의 Dancing_Script로 실었다. 다른 필기체 후보는 Caveat(펜 글씨), Sacramento(가는 흘림), Parisienne·Great Vibes(격식 흘림), La Belle Aurore·Homemade Apple(진짜 손글씨 느낌)이다.
- 조립본·Next.js·미리보기 Artifact 셋 다 반영. tsc·eslint 통과, 로컬 200.

### 2026-09-06 19:30:40 이전 · 이산하 (T14)

- 봉투가 왼쪽 아래로 15도쯤 기울어 있고, 편지지는 꺼내면서 15도 기울어 있다가 수평이 되도록 할 수 있는가.

### 2026-09-06 19:30:40 · Claude (T14)

- 무대(봉투·편지지·표제를 담은 상자)를 통째로 -15도 돌려 봉투가 왼쪽 아래로 기울었다. 편지지는 봉투 축을 따라(기울어진 방향으로) 빠져나오다가, 0.35초 뒤부터 0.6초에 걸쳐 +15도 되돌아 수평으로 펴진다. 표제도 같은 경로를 따른다. 편지지가 왼쪽 위로 빠져나오므로 무대를 오른쪽으로 24px 옮겨 균형을 맞췄다.
- 구현은 CSS의 개별 변형 속성(`translate`·`rotate`)으로 이동과 회전을 따로 전환한다. 확대 단계에서 표제가 커버 자리로 가는 이동량은 화면 좌표로 재서 무대 좌표계(-15도)로 돌려 넣는다.
- 조립본·Next.js·미리보기 Artifact 셋 다 반영. tsc·eslint 통과, 로컬 200과 CSS 반영 확인.

### 2026-09-06 19:34:24 이전 · 이산하 (T15)

- 좋다. 다음 화면 Scene2를 만들자. 디자인 에셋은 추가해 두었다. 와이어프레임 기준 3쪽이다.
- Scene1에서 봉투는 중앙에 있고, 편지지는 빠져나오면서 그 위로 와서 화면 중앙에 가지런히 있는 편이 낫겠다.

### 2026-09-06 19:34:24 · Claude (T15)

- 에셋 확인. `docs/design/scene2/scene2--opened-paper.PNG`(817×1438, 투명 배경). 레이스 장식이 달린 열린 봉투에서 긴 편지지가 올라와 있는 일러스트다. 와이어프레임 3쪽의 아치 프레임과 양옆 다이컷 오브제 대신 이 일러스트를 쓰는 것으로 읽고, 편지지의 빈 자리(이미지 세로 13~59%, 가로 22~78%)에 "2026년 10월 9일 금요일 / 오후 6시 30분 / (오크색 짧은 선) / 더채플앳청담 / 3층 커티지홀"을 얹었다. 블록은 1화면 고정이고 일러스트는 가운데 정렬, 너비는 340px과 화면 너비 86%, 화면 높이에 맞춘 값 중 작은 것이다. 축소본 `docs/design/canvas/opened-paper.png`(720px, 465KB. 투명 배경이라 PNG 유지)를 캔버스·조립본·`public/scene2/`에 같이 쓴다.
- Scene1 진입 장면 7. 봉투를 화면 중앙에 두고, 편지지는 봉투 축을 따라 210px 빠져나온 뒤(0.7초) 봉투 위로 내려와 화면 중앙에 수평으로 놓인다(0.65초, 이때 봉투 앞면보다 위로 올라온다). 표제는 편지지가 자리 잡을 때 떠오르고, 확대는 화면 중앙에서 일어난다. 전체 약 3.85초.
- 캔버스 아트보드는 커버 아래에 Scene2를 이어 붙여 390×1688이 됐다. 조립본·Next.js·미리보기 Artifact 셋 다 반영. tsc·eslint 통과, 로컬 200. 앞서 `canvas.json` 갱신이 한 번 실패해(문자열 안 줄바꿈 표기) 다시 반영했다.

### 2026-09-06 19:36:50 이전 · 이산하 (T16)

- Scene1의 "우리의 삶을 함께 써 주신 당신께"와 이름(이산하 · 송시야)을 필기체로 바꿔 달라.

### 2026-09-06 19:36:50 · Claude (T16)

- 둘 다 나눔손글씨 펜(Nanum Pen Script)으로 바꿨다. 손글씨 글꼴은 같은 픽셀에서 작아 보여서 문장은 15px → 21px, 이름은 24px → 36px(굵기 400, 자간 0.06em)로 키웠다. "그리고"는 Noto Sans KR 그대로. 진입 장면의 편지지 문장도 같은 글꼴을 따른다. Next.js에는 `next/font/google`의 Nanum_Pen_Script로 실었고, 조립본 글꼴 패널의 "이름·문장" 목록 기본값도 이 글꼴로 맞췄다(고운바탕 등 다른 후보는 목록에 남아 있어 바꿔 볼 수 있다).
- 조립본·Next.js·캔버스·미리보기 넷 다 반영. tsc·eslint 통과, 로컬 200.

### 2026-09-06 19:49:35 이전 · 이산하 (T17)

- 진입 장면을 이렇게 바꿀 수 있는가. ① 양쪽으로 열리는 편지(참고 영상 캡처 2장: 질감 있는 흰 종이 두 쪽이 가운데서 맞닿고 새틴 리본으로 묶여 있음) ② 스르륵 풀리는 리본 ③ 좌우로 열리는 편지지 ④ 안쪽 전체 화면에 WEDDING INVITATION과 "우리의 삶을 함께 써 주신 당신께"는 그대로.

### 2026-09-06 19:49:35 · Claude (T17)

- 봉투 장면을 걷어내고 리본 표지로 바꿨다. 순서는 ① 질감 있는 아이보리 종이 두 쪽이 가운데 접선에서 맞닿아 화면을 채우고, 52% 높이에 새틴 띠와 리본 매듭이 있다 ② 0.7초 뒤 리본이 풀린다(고리가 매듭 쪽으로 오므라들며 사라지고 꼬리가 아래로 흘러내리며 옅어지고, 띠는 양쪽으로 8px 느슨해진다. 0.9초) ③ 1.8초에 두 쪽이 바깥 가장자리를 축으로 105도 젖혀지며 열린다(1.2초) ④ 안쪽(커버 바탕색)에 표제와 문장이 떠오르고 ⑤ 3.6초에 커버 제자리로 옮겨 간 뒤 덮개가 걷힌다. 전체 약 4.5초. "Thanks to everyone."은 오른쪽 표지 아래에 남겼다.
- 종이 질감은 SVG 노이즈(feTurbulence)를 데이터 URI로 깔아 외부 파일 없이 냈다. 리본은 SVG로 그렸다(고리 2·매듭·꼬리 2, 새틴 그라데이션). 사진처럼 사실적인 리본은 그림이 필요하다. 묶인 리본과 풀린 리본을 투명 배경 PNG로 주면 그 두 장을 교차시키는 방식으로 바꿀 수 있다.
- 조립본·Next.js·미리보기 Artifact 반영. 캔버스는 정적이라 변경 없음. tsc·eslint 통과, 로컬 200.

### 2026-09-06 19:50:34 이전 · 이산하 (T18)

- Scene1의 "우리의 삶을 함께 써 주신 당신께"와 이산하 · 송시야를 고운바탕으로.

### 2026-09-06 19:50:34 · Claude (T18)

- 둘 다 고운바탕으로 되돌렸다. 문장 15px, 이름 24px(굵기 700, 자간 0.1em). 조립본 글꼴 패널 기본값도 고운바탕. 조립본·Next.js·캔버스·미리보기 반영, tsc 통과, 로컬 200.

### 2026-09-06 19:52:26 이전 · 이산하 (T19)

- 첫 페이지 진입 장면을 이전의 봉투 형태로 다시 바꿔 달라.

### 2026-09-06 19:52:26 · Claude (T19)

- 리본 표지(장면 8)를 걷어내고 봉투(장면 7. 화면 중앙의 15도 기운 봉투, 덮개 열림, 편지지가 축을 따라 나왔다가 봉투 위 중앙에 수평으로 놓임, "Thanks to everyone." 문구)로 되돌렸다. 커밋 9b08961의 조립본에서 진입 장면 부분만 가져왔고 글꼴은 T18의 고운바탕을 유지한다. 리본 표지 판은 논의록과 커밋 58d4521에 남아 있다.
- 조립본·Next.js·미리보기 반영. tsc·eslint 통과, 로컬 200.

### 2026-09-06 19:54:40 이전 · 이산하 (T20)

- Scene1의 사진을 잘라 넣은 창이 아니라, 자르지 않고 화면 전체를 차지하게 해 달라. 신랑신부는 그 안에서 가운데, 글씨 영역은 뒤에 덮개(overlay)를 넣어서라도 보이게.

### 2026-09-06 19:54:40 · Claude (T20)

- 커티지홀 사진을 커버 한 화면(100dvh) 전체에 깔았다. 사진 비율(366×780, 0.47)이 폰 화면(390×844, 0.46)과 거의 같아 잘리는 부분이 거의 없다. 둥근 창과 종이 테두리는 없앴다.
- 두 사람 컷아웃은 화면 가로 중앙(+10px 유지), 세로는 아래에서 38% 지점에 발을 두고 높이 34%로 세웠다(통로 위). 발밑 그림자 유지.
- 글씨 뒤 덮개는 위아래 그라데이션이다. 위쪽 34%는 종이색(#F3ECDF) 94% → 0, 아래쪽 36%는 그 반대. WEDDING INVITATION과 문장은 위 그라데이션 위에, 이름은 아래 그라데이션 위에 놓여 사진과 부딪히지 않는다. 텍스트는 위아래로 벌려(space-between) 배치했다.
- 조립본·Next.js·캔버스·미리보기 넷 다 반영. tsc·eslint 통과, 로컬 200.

### 2026-09-06 19:57:12 이전 · 이산하 (T21)

- 디자인적으로 이것이 최선인가. 디자인 어워드를 받은 디자이너라고 생각하고 진행해 보라.

### 2026-09-06 19:57:12 · Claude (T21)

- 진단. T20의 커버는 요청대로 배치한 것이지 디자인한 것이 아니었다. 약점 셋. ① 사진 위에 오려 붙인 두 사람이 합성처럼 보였다(스티커식 그림자, 빛 불일치). ② 위아래 두꺼운 아이보리 띠가 사진을 납작하게 눌렀다. ③ 이름이 주인공이 아니었다.
- 재설계, 한 가지 생각 "종이 무대 위의 두 사람". ① 홀 사진을 한 톤 물러나게 했다(채도 0.9, 대비 0.97, 종이색 16% 워시, 가장자리 비네트). 두 사람과 글자가 앞으로 나온다. ② 두 사람 뒤에 홀 조명 색의 따뜻한 빛무리를 깔고, 발밑에는 접촉 그림자(얇고 진한 것)와 바닥 그림자(넓고 옅은 것)를 따로 두어 통로 위에 서 있게 했다. 스티커식 8px 그림자는 뺐다. 키는 화면의 40%, 발은 아래에서 30%. ③ 위아래 띠는 얇고 부드러운 그라데이션으로 바꿨다(위 30%, 아래 34%, 중간 농도 55~60%). ④ 이름을 30px 고운바탕 700, 자간 0.14em으로 키워 주인공으로 삼고, "그리고"는 12px에 양옆 오크색 짧은 선을 붙여 활자 장식으로 만들었다. WEDDING INVITATION은 20px 500, 자간 0.34em으로 가늘게 하고 아래 26px 오크 선을 두었다. ⑤ 진입 장면이 끝나면 홀 사진이 16초에 걸쳐 1.07배에서 1배로 아주 천천히 물러나고, 두 사람이 1초 동안 떠오른다. 동작 줄이기 설정이면 둘 다 없다.
- 남는 한계. 두 사람(수채 일러스트)과 홀(렌더링)은 그림 문법이 달라 완전히 한 장면이 되지는 않는다. 가장 좋은 답은 두 사람이 홀 안에 서 있는 그림 한 장(같은 손, 같은 빛)을 만드는 것이다. 그 그림이 오면 지금 구조(사진 전체 + 위아래 그라데이션 + 활자)에 그대로 갈아 끼운다.
- 조립본·Next.js·캔버스·미리보기 반영. tsc·eslint 통과, 로컬 200.

### 2026-09-06 19:59:40 이전 · 이산하 (T22)

- "종이 무대 위의 두 사람" 콘셉트는 좋다. 지금 사람 크기가 너무 크다. 크기는 지금의 2/3, 위치는 좀 아래에 버진로드 위에 서 있는 것처럼.
- WEDDING INVITATION 글씨는 작게, "우리의 삶을 함께 써 주신 당신께"는 좀 더 크게.
- 배경 이미지 바깥이 점점 옅어지는 것은 좋은데 좀 더 빠르게 옅어졌으면.

### 2026-09-06 19:59:40 · Claude (T22)

- 두 사람 키를 화면의 40% → 27%로 줄이고, 발을 아래에서 22% 지점(통로 앞쪽)에 두었다. 빛무리·접촉 그림자·바닥 그림자도 같이 옮기고 줄였다. 아래 그라데이션은 발이 종이에 묻히지 않도록 높이 34% → 30%로 낮췄다.
- WEDDING INVITATION 20px → 14px(자간 0.36em), 문장 15px → 20px.
- 가장자리 옅어짐을 앞당겼다. 비네트를 어두운 갈색에서 종이색으로 바꾸고 중심에서 34% 지점부터 옅어지기 시작해 가장자리에서 85%가 되게 했다. 위아래 그라데이션도 40~45% 지점에서 이미 80% 농도가 되도록 곡선을 세웠다.
- 조립본·Next.js·캔버스·미리보기 반영. tsc 통과, 로컬 200.

### 2026-09-06 20:01:58 이전 · 이산하 (T23)

- Scene1 배경의 홀 사진 불투명도를 1로 통일해 달라.

### 2026-09-06 20:01:58 · Claude (T23)

- 홀 사진 위에 얹혀 있던 종이색 워시(16%), 가장자리 비네트, 위아래 그라데이션, 두 사람 뒤 빛무리, 채도·대비 필터를 전부 걷어냈다. 사진은 화면 전체에 원본 그대로 보인다. 남긴 것은 두 사람 발밑의 접촉 그림자와 바닥 그림자뿐이다(사진 불투명도가 아니라 두 사람의 그림자).
- 글자(WEDDING INVITATION · 문장 · 이름)는 이제 사진 위에 바로 놓인다. 커튼과 통로가 밝은 아이보리라 지금 크기에서는 읽히지만, 폰에서 보고 부족하면 글자 뒤에 작은 종이 라벨을 두는 안을 낼 수 있다.
- 조립본·Next.js·캔버스·미리보기 반영. tsc·eslint 통과, 로컬 200. 첫 실행에서 빛무리 CSS 블록 대조가 어긋나 파일을 쓰지 못했고, 대조를 고쳐 다시 반영했다.

### 2026-09-06 20:03:40 이전 · 이산하 (T24)

- 배경 이미지 색감을 Claude가 바꾼 것 아닌가. 처음에 준 것으로 다시 해 달라.

### 2026-09-06 20:03:40 · Claude (T24)

- 의도한 색 보정은 T21의 채도·대비 필터와 종이색 워시였고 T23에서 이미 걷어냈다. 남아 있던 차이는 축소본이 JPEG(품질 72, 366px)였다는 점이다. 원본 PNG는 색 프로파일이 없고(브라우저는 sRGB로 취급) JPEG는 sRGB 태그가 붙었으니 색 공간 차이는 없지만, JPEG 압축과 낮은 해상도가 질감을 무디게 했을 수 있다.
- 조치. 조립본은 원본 `docs/design/scene1/scene1--wedding-hall.PNG`(860×1829)를 그대로 참조한다. Next.js에는 원본을 `public/scene1/hall.png`로 복사했다(JPEG 삭제). 캔버스는 2MB 항목 제한 때문에 원본을 무손실로 900px로만 줄인 `docs/design/canvas/hall.png`(519KB)를 쓴다. 미리보기 Artifact는 스크립트가 원본을 1080px로 무손실 축소해 심는다(1.8MB).
- 조립본·Next.js·캔버스·미리보기 반영. 로컬 200, 이미지 200.

### 2026-09-06 20:05:26 이전 · 이산하 (T25)

- Next.js 임시 작업분도 바꿨는가. (확인: 바꿨고 원본과 동일)
- 배경 채도를 좀 뺄 수 있는가. 필터 같은 것으로.

### 2026-09-06 20:05:26 · Claude (T25)

- 홀 사진에 CSS 필터 `saturate(0.7)`를 걸었다. 원본 파일은 그대로이고 화면에서만 채도가 70%다. 수치는 바꾸기 쉽다. 조립본·Next.js·캔버스·미리보기 반영, 로컬 200.

### 2026-09-06 20:06:14 이전 · 이산하 (T26)

- WEDDING INVITATION 글자 크기는 지금의 2/3으로, "우리의 삶을 함께 써 주신 당신께"는 좀 더 크게. 아래 이산하 · 그리고 · 송시야 사이 여백을 약간 더.

### 2026-09-06 20:06:14 · Claude (T26)

- WEDDING INVITATION 14px → 10px(자간 0.36em 유지), 문장 20px → 24px(줄 간격 1.5), 이름 사이 간격 2px → 10px. 조립본·Next.js·캔버스·미리보기 반영, 로컬 200.

### 2026-09-06 20:08:44 이전 · 이산하 (T27)

- WEDDING INVITATION과 문장 사이의 짧은 선, "그리고" 양옆의 선을 지워 달라.

### 2026-09-06 20:08:44 · Claude (T27)

- 둘 다 지웠다. 표제 아래 26px 오크 선과 "그리고" 양옆 18px 선. 조립본·Next.js·캔버스·미리보기 반영, 로컬 200.

### 2026-09-06 20:10:21 이전 · 이산하 (T28)

- 커밋 안 된 것이 있는가. 한번 배포하려고 한다. 배포는 이산하가 하니 커밋 안 된 것만 넣어 달라.

### 2026-09-06 20:10:21 · Claude (T28)

- 커밋 안 된 것은 WIW-4의 Next.js 임시 적용 코드(`src/app/layout.tsx` `page.tsx` `globals.css`)와 이미지 3장(`public/scene1/hall.png` `couple.png` `public/scene2/opened-paper.png`)뿐이다. `next build`가 통과했다(정적 페이지 2개). WIW-4는 원래 "커밋하지 않음"이었으므로 이 지시를 근거로 범위를 넓혀 문서에 적은 뒤 코드를 커밋한다. develop → main PR과 배포는 이산하 몫이다.
- 주의. 이미지는 원본 그대로라 커버 PNG가 1.9MB다. 배포 확인용으로는 되지만 발송본은 이미지 파이프라인(webp 변형)을 거쳐야 한다.

### 2026-09-06 20:12:56 이전 · 이산하 (T29)

- 배경 이미지 상단을 좀 흐릿하게. 4분의 1 정도만, 오버레이로 중앙에서 멀어질수록 흐릿하게.
- compact나 세션을 다시 열어도 작업을 이어갈 수 있는 상태로 만들어 달라.

### 2026-09-06 20:12:56 · Claude (T29)

- 상단 흐림. 배경 흐림(backdrop-filter) 오버레이 두 겹을 겹쳤다. 위 26%는 5px 흐림, 위 16%는 9px 흐림이고 각각 마스크로 아래쪽이 투명해져서, 위로 갈수록 더 흐리고 중앙 쪽은 선명하다. 사진 파일은 그대로다.
- 이어가기 준비. ① 조립본 CSS를 Next.js로 옮기는 절차를 `docs/scripts/sync-globals.mjs`로 만들었다(조립본이 정본, globals.css는 파생물). ② `docs/artifacts/README.md`를 지금 상태·링크 두 개·파일 자리·고치는 순서·다시 올리는 법으로 다시 썼다. ③ `CLAUDE.md` 라우터 상태 줄을 "Scene1·2 완료, 다음 Scene3"로 바꾸고 README를 가리키게 했다. ④ 코드(흐림 반영분)까지 develop에 커밋해 작업 트리를 비웠다. 새 세션은 CLAUDE.md → artifacts README → 이 논의록 마지막 T-로그 순으로 읽으면 된다.

### 2026-09-06 20:17:23 이전 · 이산하 (T30)

- (스크린샷) 배경 이미지 상단 흐림 효과가 적용되지 않았다.

### 2026-09-06 20:17:23 · Claude (T30)

- 원인. 조립본 CSS에 표준 `backdrop-filter`와 `-webkit-backdrop-filter`를 나란히 썼는데, Tailwind v4의 CSS 처리기(Lightning CSS)가 둘을 합치면서 표준 선언을 떨어뜨렸다. Next.js가 내려 준 CSS에는 접두사 선언만 남았고 Chrome은 그 접두사를 모르므로 흐림이 사라졌다. 조립본 파일을 직접 열면 정상이었다.
- 조치. `docs/scripts/sync-globals.mjs`가 `-webkit-backdrop-filter`·`-webkit-mask-image` 중복을 걷어내고 표준만 넘기게 했다. 처리기가 접두사를 알아서 붙인다. Playwright로 Next.js 화면을 열어 계산된 스타일이 `blur(9px)`이고 상단 4분의 1이 흐려진 것을 스크린샷으로 확인했다.
- 덤. 그 스크린샷에서 두 사람 컷아웃 가장자리에 자홍색 테두리 잔상이 보였다. 원본 PNG의 반투명 픽셀에 자홍색이 남아 있던 것이다. `docs/scripts/unmatte.py`(반투명 픽셀 색을 이웃 불투명 픽셀 색으로 교체)를 만들어 정리본 `docs/design/scene1/scene1--married-couple--clean.png`을 만들고, 캔버스(400px)와 Next.js(700px) 축소본을 거기서 다시 만들었다. 원본은 그대로다.
- 조립본·Next.js·캔버스·미리보기 반영. 코드(`globals.css`·`public/scene1/couple.png`)도 develop에 커밋한다.

### 2026-09-06 21:03:10 이전 · 이산하 (T31)

- 디자인 작업을 이어간다. design 스킬 없이 디자인을 더 잘하는 방법이 있는지, 앤틱한 PNG 에셋을 만들 수 있는지 물었다.
- 청첩장 콘셉트는 팝업북이다. Scene1의 두 사람과 배경이 팝업북 형태이니 처음 등장 애니메이션도 같은 콘셉트로 만들어 달라.
- 빈티지를 살짝 섞고 싶다. 빈티지는 HTML·CSS로 흉내 내면 어색하니, 앞서 보인 표본처럼 PNG 에셋으로 만들어 넣는 방향이 좋다.
- Artifact로는 앞으로 게시하지 않는다. 그것 없이도 디자인할 수 있다고 했으니.
- 첫 Scene에서 신랑·신부 이미지가 나올 때의 애니메이션을 없앤다. 로딩(진입) 애니메이션과 첫 씬이 자연스럽게 이어지면 좋겠다.

### 2026-09-06 21:03:10 · Claude (T31)

- 에셋 길. 이 맥에서 바로 되는 길이 둘이다. ① SVG 필터(feTurbulence 종이 결, feDisplacementMap 찢은 가장자리, 잉크 번짐)를 Chrome으로 그려 투명 PNG로 뽑는 길 ② Pillow·numpy로 사진을 낡게 후처리하는 길. 낡은 라벨 카드 표본을 스크래치패드에 만들어 보였다. 이미지 생성 모델은 없어서 채색 캐릭터 일러스트는 못 만든다(Q11 그대로). 퍼블릭 도메인 스캔을 받아 정리하는 길은 라이선스를 건별로 본다.
- 진입 장면 9 · 팝업북. 봉투(장면 7)를 걷어내고 팝업북으로 바꿨다. 순서는 ① 닫힌 책(빈티지 천 표지에 표제·문장·"Thanks to everyone.")이 위에서 내려다보는 시점(55도)으로 떠오른다 ② 0.6초에 표지가 책등을 축으로 왼쪽으로 젖혀지고 책이 가운데로 온다(1.3초). 표지가 세로로 설 때까지 접힌 조각은 보이지 않는다 ③ 1.45초에 홀 배경 조각이 종이처럼 일어서고(1초, 살짝 넘쳤다 서는 곡선) 0.2초 뒤 두 사람이 일어선다(0.85초). 접혀 있을 때는 종이색 뒷면이 보인다. 바닥에 조각이 드리우는 띠 그림자와 발밑 타원이 생긴다 ④ 2.75초에 시점이 내려오며 무대가 화면을 채운다(1.2초). 기울기 0도, 소실점이 바닥선(아래 22%)으로 내려오고 두 사람은 홀 조각 평면으로 물러나 커버의 두 사람과 같은 자리·크기가 된다. 홀 조각은 장면 동안 선 채로 봤을 때 윗부분 46%를 잘라 책 깊이에 맞췄다가 이때 끝까지 편다. 바탕은 1초 뒤에야 걷어 아래 커버가 비친다 ⑤ 3.95초에 덮개가 걷히고 표제·이름이 떠오른다(0.8초). 약 4.3초. 건너뛰기·다시 보기·동작 줄이기 대응은 그대로다.
- 첫 씬 이어짐. 커버에서 두 사람이 떠오르던 애니메이션(T25의 1초 떠오름)을 뺐다. 팝업에서 일어선 두 사람이 마지막 프레임에서 커버의 두 사람과 같은 자리·크기로 겹치므로 덮개가 걷혀도 두 사람은 움직이지 않는다. 홀 사진의 16초 물러남은 장면 안의 홀 조각도 같은 배율(1.07)로 시작해 이어진다. 표제·이름만 걷힐 때 0.8초 떠오른다.
- 기하. 무대는 커버와 같은 크기이고 홀 조각은 커버 홀 사진의 위 78%(두 사람 발 위치까지)를 같은 배율로 담는다. 그래서 장면 끝 프레임이 커버와 픽셀 단위로 겹친다. 책장은 폭의 0.65 × 0.94배(비율 0.69), 두 사람은 책장 깊이의 35% 지점에 선다.
- 빈티지 에셋. 표지 `public/intro/book-cover.png`(천 결·세월 얼룩·닳은 모서리·책등 그늘·경첩 홈·이중 괘선·모서리 문양·위아래 꽃무늬)와 속지 `public/intro/page.png`(섬유 결·옅은 얼룩·갈색 점·가장자리 그늘)를 SVG로 그려 `docs/scripts/render-asset.mjs`(playwright-core + 설치된 Chrome)로 1.5배 PNG로 뽑고 `docs/scripts/quantize-png.py`로 256색으로 줄였다(48KB · 18KB). 원본 SVG는 `docs/artifacts/assets/`에 커밋한다. 표제 글자는 HTML로 얹고 괘선 가운데에 맞췄다. 정도는 "살짝"이라 첫 렌더에서 얼룩 농도를 한 단계 낮췄다. 속지는 오른쪽 면, 표지 안쪽 면, 홀 조각 뒷면에 같이 쓴다.
- 처리기 함정 둘. ① 3D 컨테이너에 clip-path를 걸면 평면화되어 앞면이 그려지지 않는다. 자르기를 면에 걸었다. ② Tailwind v4의 처리기(Lightning CSS)가 개별 변환 속성 `translate`를 떨어뜨려 Next.js에서 무대가 오른쪽으로 밀리고 조각이 사라졌다. transform 함수로 바꾸고 `sync-globals.mjs`에 개별 변환 속성 게이트를 넣었다. `-webkit-backface-visibility` 중복도 걷어내게 했다.
- 확인. Playwright로 조립본(file://)과 Next.js(localhost:3000) 둘 다 0.15 · 1.0 · 1.4 · 1.7 · 2.3 · 3.0 · 3.6 · 4.4초 프레임을 찍어 같은 순서로 동작하는 것을 봤다. 계산된 스타일로 backface-visibility · preserve-3d · 흐림 · 자르기가 살아 있는 것도 봤다. tsc · eslint 통과. 미리보기 Artifact를 같은 링크에 한 번 다시 올렸다(라벨 "진입 장면 10 · 팝업북"). 이 발행 뒤 이산하가 Artifact 발행 중단을 지시했다. 캔버스는 바꾸지 않았다.
- 결정 11. Artifact·캔버스 발행을 중단한다. 시안은 조립본 `docs/artifacts/index.html`과 Next.js 로컬 적용으로 만들고, 이산하는 로컬(`next dev`)과 조립본 파일을 브라우저·폰에서 확인하며, Claude는 Playwright 프레임 캡처로 검수한다. 결정 4 · 7 · 10은 폐기한다. 기존 링크 두 개는 이 시점 상태로 동결한다. `CLAUDE.md` 상태 줄과 `docs/artifacts/README.md`를 그에 맞게 고쳤다.
- 의존성. `playwright-core`를 devDependency로 더했다(스택 결정 10. 브라우저를 내려받지 않고 설치된 Chrome을 쓴다).
- 코드(`src/app/` · `public/intro/` · `package.json`)는 WIW-4 범위(임시 적용 코드를 develop에 커밋)대로 develop에 커밋한다.

### 2026-09-06 21:28:32 이전 · 이산하 (T32)

- 처음 책은 빈티지가 아니라 앤티크한 책이다. 서양 책 느낌, 고급스러운 붉은 가죽 표지.
- 책에 두께감이 없어 아쉽다. 에셋으로 대체해서 할 수 있는가.
- 책이 펼쳐지고 신랑신부 이미지가 똑바로 선 상태에서 올라오는데, 실제 팝업북이면 위아래가 뒤집혀 있을 것이다. 뒤집혔을 때는 그냥 종이 뒷면처럼 보였으면 한다. 배경이 될 부분도 마찬가지.
- 로딩이 끝나고 첫 화면으로 자연스럽게 이어져야 하는데, 아래 이름 부분이 첫 Scene이 나올 때 잘리는 느낌이 있다. 나머지도 마찬가지. 로딩에서 첫 Scene까지 자연스럽게 연결할 수 있는가.

### 2026-09-06 21:28:32 · Claude (T32)

- 표지. 붉은 가죽(#7B2431 바탕. 노이즈를 요철로 보고 빛을 비춘 자갈결, 저주파 얼룩, 가장자리 어두움, 닳은 모서리는 밝은 갈색)에 금박 이중 괘선·모서리 덩굴·위아래 꽃무늬(금 그라데이션 + 눌린 자국 그늘)를 `docs/artifacts/assets/book-cover.svg`로 다시 그려 `public/intro/book-cover.png`(75KB)로 뽑았다. 제목·문장·"Thanks to everyone."은 HTML로 금색(#DCC07A · #EAD79C · #D9BC6B, 아래쪽 0.6px 어두운 그림자)으로 얹는다. 문구는 아래 꽃무늬와 안쪽 괘선 사이로 올렸다.
- 두께. 책을 가운데서 펼친 두 뭉치로 만들었다. 오른쪽 뭉치는 고정이고 앞쪽 반(표지 판 4px + 종이 14px)이 책등을 축으로 통째로 젖혀진다. 종이 면이 바닥선에 놓이고 뭉치는 그 아래로 내려가므로 조각의 기하는 그대로다. 보이는 옆면은 앞면(보는 쪽)과 책배(바깥쪽) 둘이고, 종이 결은 `assets/page-edges.svg` → `public/intro/page-edges.png`(가로줄 겹침, 83KB), 표지 판은 가죽색 띠다. 책 아래에 흐린 그림자를 두고 왼쪽 것은 젖혀질 때 나타난다. 세계를 16도(열리면 10도) 돌려 책배가 보이게 했다가 시점이 내려올 때 0도로 돌아온다. 그림 한 장으로 대체하면 젖혀지는 동작이 안 되므로 면마다 그림을 입힌 3D로 했다.
- 뒤집힌 뒷면. 두 사람 조각은 종이 면에 앞면을 대고 접혀 있어 접힌 동안 뒷면이 위를 보고 머리가 보는 쪽에 온다. 뒷면은 `docs/scripts/paper-back.py`가 속지 결을 두 사람 윤곽으로 오리고 위아래를 뒤집은 `public/intro/couple-back.png`(5KB)다. 브라우저 마스크(CSS mask · 인라인 SVG mask)를 먼저 써 봤는데 file://에서 막히거나 3D 변환 안에서 그려지지 않아 그림 파일로 두었다. 홀 조각의 뒷면은 속지 결 그대로다. 두 뒷면 다 종이 면보다 반 톤 어둡고 가장자리에 얇은 그늘이 있어 종이 위에 놓인 다른 종이로 읽힌다.
- 이어짐. 잘리는 느낌의 원인은 셋이었다. ① 표제·이름이 장면이 걷힌 뒤에 따로 떠올랐다 ② 아래 22%가 무대 바탕색에서 통로 사진으로 0.3초에 바뀌었다 ③ 메뉴 버튼이 걷힘과 함께 튀어나왔다. 고친 것은 ① 표제·이름 사본을 무대 안에 커버와 같은 배치로 두고 시점이 내려올 때(2.75 + 0.85초부터 0.7초) 떠오르게 했다. 커버의 원본은 장면 동안 숨겼다가 걷힐 때 0.35초로 나타나므로 사본과 겹쳐 바뀐다 ② 통로(홀 사진 아래 22%)와 발밑 그림자 사본을 무대 안 같은 자리에 두고 시점이 내려올 때 떠오르게 했다. 무대 바탕은 끝까지 불투명하다(비치면 움직이는 두 사람 뒤로 커버의 두 사람이 겹쳐 보인다) ③ 메뉴 버튼은 걷힌 뒤 0.25초 뒤부터 0.6초 동안 떠오른다. 걷히기 직전(3.95초)과 걷힌 뒤(4.3초) 프레임을 픽셀로 비교했더니 차이는 홀 사진의 16초 물러남(1~2px)과 메뉴 버튼뿐이었다.
- 함정 하나 더. 책 뭉치의 클래스를 `.block`으로 지었더니 페이지 구획(`section.block`)과 부딪혀 커버가 흐름에서 빠졌다. `.stack`으로 바꿨다.
- 확인. 조립본(file://)과 Next.js(localhost:3000) 프레임 10장씩, 펼친 면을 위에서 내려다본 컷, 닫힌 책 확대 컷. tsc · eslint 통과. Artifact는 올리지 않는다(결정 11).
- 코드는 WIW-4 범위대로 develop에 커밋한다.

### 2026-09-06 21:46:15 이전 · 이산하 (T33)

- Scene1이 등장할 때의 애니메이션을 전부 없애면 어떨까.
- (Claude의 답 뒤) 그렇게 하자. "표제를 홀 조각에 인쇄한다"는 말이 무슨 뜻인지 물었고, 설명이 이상하다고 했다.
- (풀어 쓴 설명 뒤) 진짜 팝업북에서 배경 그림에 제목이 같이 찍혀 있는 것처럼 가자.

### 2026-09-06 21:46:15 · Claude (T33)

- 판단. 팝업북이 이미 등장 연출이라 그 뒤의 움직임은 군더더기다. Scene1은 정지, 움직임은 전부 진입 장면 안에서 끝나게 한다.
- 홀 사진의 16초 물러남(T25)을 뺐다. 커버에는 이제 애니메이션이 없다. 재시작 코드도 사라졌다.
- 표제("WEDDING INVITATION"과 문장)를 홀 조각의 윗부분에 찍었다(`.pop__title`. 커버의 표제와 같은 자리, 위 56px + 안전 영역). 장면 동안은 조각의 위 46%가 잘려 있어 보이지 않다가, 시점이 내려오며 조각이 끝까지 펴질 때 같이 드러난다. 따로 떠오르는 표제 사본은 없앴다. 이름은 통로 자리라 조각에 붙일 수 없어 사본으로 남기되, 통로·발밑 그림자와 같은 박자(2.75 + 0.55초부터 0.5초)로 떠오르게 해 하나의 인쇄물처럼 나타난다.
- 메뉴 버튼은 걷힘과 같은 0.35초에 함께 나타난다(전에는 0.25초 뒤 0.6초).
- 남아 있던 어긋남을 찾았다. 조각을 종이에서 띄우려고 준 translateZ 1.5px·2px가 회전 앞에 있어 바닥 좌표의 위쪽으로 작용했고, 서 있는 조각이 화면 위로 1.8px·2.3px 밀려 있었다. translateZ를 회전 뒤로 옮겨(접혀 있을 때만 종이에서 뜨고 서면 보는 쪽으로 향함) 시점이 내려올 때 0으로 되돌렸다.
- 확인. 걷히기 직전(3.94초) 요소 상자를 커버와 비교했더니 홀·두 사람·표제·이름·발밑 그림자 모두 0.3px 이내(대부분 0.00)다. 걷히기 직전(3.95초)과 걷힌 뒤(4.4초) 프레임의 픽셀 차이는 메뉴 버튼을 빼면 0(최대 채널 차 14/255)이다. 프레임 캡처는 캐시를 데운 뒤 다시 열어 `is-shown`이 붙는 순간을 기준 시각으로 잡도록 고쳤다(전에는 이미지 로딩 시간만큼 늦게 찍혔다). tsc · eslint 통과.
- 코드는 WIW-4 범위대로 develop에 커밋한다.

### 2026-09-06 21:53:16 이전 · 이산하 (T34)

- 로딩 관련된 것을 전부 삭제해 달라. 로딩은 처음부터 다시 한다. Scene1만 남겨 두라.

### 2026-09-06 21:53:16 · Claude (T34)

- 진입 장면(로딩)을 전부 걷어냈다. 조립본의 진입 장면 CSS·마크업·스크립트, 글꼴 패널의 "진입 장면 다시" 버튼, 봉투 색 토큰과 필기체 글꼴 토큰(Dancing Script. 진입 장면의 "Thanks to everyone."에만 쓰였다), Next.js의 `page.tsx` 진입 장면 부분과 `layout.tsx`의 Dancing_Script, `sync-globals.mjs`의 그 글꼴 매핑, 에셋 `public/intro/`(가죽 표지·속지·책배·두 사람 뒷면)와 원본 `docs/artifacts/assets/`. 페이지는 Scene1 커버로 바로 시작한다.
- Scene2(핵심 정보)는 로딩과 무관하므로 그대로 두었다. "Scene1만"이 Scene2도 지우라는 뜻이면 한 줄이다.
- 남긴 것. 스크립트 셋(`render-asset.mjs` · `quantize-png.py` · `paper-back.py`)은 다음 판에도 쓰이므로 두고, 문서의 예시 경로만 일반형으로 바꿨다. 걷어낸 판은 커밋 1f1a819(조립본·코드)와 6d7dc2e(에셋 원본)에 남아 있고, 배운 것(3D 컨테이너에 clip-path·overflow·opacity 금지, 3D 안에서 마스크 대신 그림 파일, 클래스 이름 충돌, 마지막 프레임을 커버와 픽셀 단위로 맞추기)은 `docs/artifacts/README.md`에 적어 두었다.
- 확인. Next.js가 커버를 정지 화면으로 바로 내려 주고 콘솔 오류가 없는 것을 스크린샷으로 봤다. tsc · eslint 통과.
- 코드는 WIW-4 범위대로 develop에 커밋한다.

### 2026-09-06 22:12:52 이전 · 이산하 (T35)

- 로딩 화면을 다시 만든다. 구현하기 전에 디자인 에셋부터 함께 만든다. 편지봉투를 만들되 실제 편지지처럼 접은 흔적이 있어야 하고, 종이 질감에 가운데는 밀랍 봉인이다. SVG + 필터 → Chrome 렌더 → 투명 PNG로 만든다. 마음에 들면 로딩 화면에 그 에셋을 쓴다. 참고 사진은 아이보리 아마 결 봉투에 청동빛 밀랍 봉인.
- (중간) 화면 규격은 생각하지 말고 일반적인 편지봉투(약 600×400 비율)로 만들어 달라.

### 2026-09-06 22:12:52 · Claude (T35)

- 봉투를 세 장으로 나눠 그렸다. 뒷면 `envelope-back.svg`(옆 날개 두 장 + 접혀 올라온 아래 날개, 600×400), 위 날개 `envelope-flap.svg`(600×236, 뒷면의 (0,0)에 얹는다), 밀랍 봉인 `wax-seal.svg`(140×140, 봉투의 (300,205)에 가운데를 맞춘다). 나중에 날개만 들어 올리거나 봉인만 떼는 움직임을 만들 수 있도록 따로 뽑는다. 네 날개의 끝이 가운데에서 만나는 서양식 봉투이고 빛은 왼쪽 위에서 온다.
- 종이 결. 처음에는 노이즈 요철에 빛을 비추는 방식만 썼더니 마대처럼 거칠었다. 촘촘한 실 격자(2.6px 간격)를 노이즈로 살짝 흔들어 규칙적인 짜임을 내고, 그 위에 가로·세로로 긴 결의 약한 요철 빛과 옅은 세월 얼룩을 얹으니 아마 결 종이가 됐다. 날개 가장자리는 잘린 종이가 빛을 받는 흰 선, 고르게 번지는 그늘, 오른쪽 아래로 치우친 그늘의 셋으로 냈고, 접힌 곳(가장자리)으로 갈수록 살짝 어둡게 했다.
- 봉인. 회색 그림을 높이 지도로 쓰는 방식이다. 낮은 주파수 노이즈로 가장자리를 울퉁불퉁하게 하고, 요철에 빛을 비춰 명암과 광택을 얻은 뒤 청동빛(#7C4B22)을 입혔다. 도장 안에는 구슬 테와 "S & S"(Baskerville), 위아래 작은 마름모. 글자는 도장 자국이라 PNG에 굽는다(HTML로 글자를 얹는 규칙의 예외). 종이에 드리우는 그늘도 굽는다(봉인이 떼일 때 같이 움직이도록). 위 날개의 그늘은 굽지 않고 CSS drop-shadow로 얹는다(날개가 들릴 때 따라 움직이도록).
- 산출물은 `public/intro/`에 뽑아 두었고 아직 커밋하지 않았다(뒷면 152KB · 위 날개 45KB · 봉인 30KB, 256색). 보는 곳은 http://localhost:3000/intro/preview.html(임시 미리보기, 커밋하지 않는다). 원본 SVG와 이 기록만 커밋한다.
- 다음. 이산하가 마음에 들면 로딩 화면에 쓴다. 도장 글자, 봉인 색(청동 대신 진홍), 종이 색은 각각 한 줄씩 바꿀 수 있다.

### 2026-09-06 22:31:29 이전 · 이산하 (T36)

- 좋다. 로딩은 이렇게 한다. ① 시작은 모바일 화면에 맞춰 봉투 좌우가 잘리게 ② 줌아웃되며 봉투 전체가 화면에 들어오게 ③ 봉투가 열린다(위 부분 = 뚜껑 + 봉인이 위로 180° 올라감) ④ 열린 봉투에서 Scene1이 나오고, 나오면서 봉투는 사라지고 Scene1이 화면을 가득 채운다. 들어가기 전에 애매한 부분은 질문하고 확답을 받은 뒤 시작하라.
- (질문에 답) 시작 크기는 봉투 높이 = 화면 높이. 줌아웃 뒤 바깥은 미리보기의 종이색 바탕. 봉인은 뚜껑에 붙은 채로 같이 젖혀진다. Scene1은 카드가 위로 빠져나오며 커진다.

### 2026-09-06 22:31:29 · Claude (T36)

- 조립본과 Next.js에 진입 장면을 넣었다. 5초 남짓이다. 0~0.5초 봉투 높이 = 화면(배율 `--z0`, 좌우 잘림, 봉인이 가운데) → 0.5~1.7초 물러나 전체가 보임(`--z1`, 봉투 폭 = 화면 폭의 88%, 최대 380px) → 1.9~2.9초 뚜껑이 봉인을 단 채 윗변을 축으로 180° 젖혀짐(원근 1400px. 90°에서 바깥면 그림을 안쪽면 그림으로 바꾼다. 뒤에서 보면 위아래가 뒤집혀 끝이 위로 간다) → 3.0~3.8초 카드가 봉투 위 입구로 빠져나옴 → 3.8~4.9초 카드가 커져 화면을 채우고, 그동안(3.9~4.8초) 봉투는 살짝 물러나며 흐려져 사라진다.
- 카드는 커버 자체다(사본이 아니다). transform(translateY + scale, 기준점 위 가운데)으로 봉투 안에 작게 두고, 봉투 안에 든 부분은 clip-path inset으로 가린다. 커지기 시작할 때 0.3초 안에 클립을 풀어 봉투 앞으로 나온다. 마지막 키프레임은 변환·클립·필터가 없어 커버 정지 화면과 같다. 배율과 카드 값은 스크립트가 화면 크기에서 계산해 CSS 변수로 넣는다. 카드 폭은 봉투 폭의 60%.
- 뚜껑 안쪽면 에셋 `envelope-flap-inside.svg`를 더했다(모양은 바깥면과 같고 민무늬). 확대 시작 장면 때문에 뒷면·날개는 3배, 봉인은 6배로 다시 뽑았다(뒷면 694KB · 날개 205KB · 안쪽 209KB · 봉인 95KB, 256색). 시작 장면은 그래도 2.1배 확대라 종이 결이 조금 부드럽고 봉인은 또렷하다. 용량은 발송본 이미지 파이프라인 때 다시 본다.
- 건너뛰기. 화면을 탭하면 바로 커버다. 움직임 줄이기 설정이거나 앵커(#…)로 들어오면 재생하지 않는다. 매번 재생한다(세션당 1회로 바꾸려면 한 줄).
- 확인. 조립본 파일과 localhost:3000에서 Playwright 프레임 16장(0.15~5.6초)을 찍어 봤다. 두 곳의 동작이 같고, 마지막 프레임과 커버 정지 화면(움직임 줄이기로 연 것)의 픽셀 차이는 최대 채널 차 1/255(조립본) · 0(Next.js)이다. 콘솔 오류 없음. tsc · eslint 통과.
- 곁가지. 조립본에서 바로 가기 메뉴가 열린 채로 보이던 것을 고쳤다. `hidden` 속성을 `.fab__menu`의 display: flex가 이기고 있었고, Next.js는 Tailwind 초기화가 가려 줘서 멀쩡했다. `.fab__menu[hidden]`에 display: none.
- 코드는 WIW-4 범위대로 develop에 커밋한다. 임시 미리보기 `public/intro/preview.html`은 지웠다. 이제 http://localhost:3000/ 이 진입 장면부터 보여 준다.

### 2026-09-06 22:46:56 이전 · 이산하 (T37)

- ① 처음에 봉투가 딱 가운데가 아니라 오른쪽으로 치우쳐 있다. ② 봉투 열리는 것을 더 자연스럽게, 조금만 느리게. 봉투에 붙어 있던 봉인이 왜 갑자기 날아가는가. 차라리 붙어 있다가 봉투와 함께 사라지게 해 달라. ③ 봉투가 열릴 때도 화면 가운데가 아니라 오른쪽으로 치우쳐 있다. ④ 봉투가 열리기 전부터 희끗한 편지지(Scene1)가 미리 보인다. 그림자 영역 같은 것. 열린 뒤부터 보여야 한다.
- ⑤ 새로고침하면 맨 위로 스크롤되게. 지금은 내려간 채로 새로고침하면 Scene1의 내려간 부분이 그대로 열려 이상하다. ⑥ 화면 오른쪽 아래에 Skip 표시. 적당히 눈에 띄게, 과하지 않게, 지금 분위기에 맞게. 필기체도 좋겠다.
- ⑦ 편지지가 나올 때와 빠져나온 뒤 앞으로 오는 애니메이션이 어색하다. 더 자연스럽게. ⑧ 편지지는 위로 나오고 봉투는 아래로 내려가는 게 자연스럽겠다. ⑨ 줌아웃됐을 때 봉투 크기는 화면의 80% 정도, 화면 정중앙.
- (뒤이어) 왜 봉투가 열렸을 때 봉인이 따라가지 않게 바뀌었나. Skip 문구는 "넘어가기"로, 글꼴은 Scene1의 고운바탕으로.

### 2026-09-06 22:46:56 · Claude (T37)

- ①③ 치우침의 원인은 봉투 상자(600px)가 화면보다 넓어 grid 트랙이 왼쪽에서 시작한 것이다. 화면 폭과 무관하게 봉투 중심이 x=300에 있었다(390px 화면에서 105px 오른쪽). T36의 프레임에서 이것을 놓쳤다. absolute + 음수 margin으로 가운데를 맞췄고, 375 · 390 · 430 · 1000 · 1440px 폭에서 봉투 중심 = 화면 중심을 재 확인했다.
- ④ 카드(커버)의 클립 위쪽에 둔 40px 여유로 카드 그림자가 새어 봉투 윗변 위에 보였다. 위쪽 여유를 없앴다(그림자는 좌우와 아래만). 열리기 전 프레임에서 봉투 윗변 위 띠의 차이가 카드 폭 구간과 바깥 구간에서 같은 정도(평균 6.0 · 5.7)라 봉투 자체 그림자만 남은 것을 확인했다.
- ② 봉인은 뚜껑에 붙은 채로 끝까지 따라간다. 전에는 90°에서 뚜껑 바깥면과 함께 숨겨서 날아가는 것처럼 보였다. 이제 숨기지 않고 젖혀진 뚜껑 끝에 그대로 있다가(뒤집혀 보인다) 봉투와 함께 내려가 사라진다. 중간에 봉투 몸에 붙이는 판도 만들었다가 이산하가 "왜 따라가지 않느냐"고 해서 되돌렸다. 뚜껑은 1.0초 → 1.4초, 곡선 cubic-bezier(0.55, 0, 0.3, 1).
- ⑦⑧⑨ 카드 등장을 다시 짰다. 카드가 올라오는 동안 봉투가 같은 박자로 내려간다(같은 완화 곡선. 클립 선과 봉투 윗변이 둘 다 같은 매개변수에 선형이라 늘 일치한다). 봉투 윗변이 카드 아래를 지나면 클립이 저절로 풀리므로 억지로 푸는 순간이 없다. 올라온 카드는 화면 세로 가운데에 서고(윗변 = 화면 높이 × (1 − 배율) / 2), 그다음 확대는 위 가운데 기준 변환이 곧 화면 가운데 기준 확대가 되어 다가오는 느낌이다. 봉투는 그동안 더 내려가며 사라진다. 봉투 폭은 화면 폭(페이지 폭 430까지)의 80%.
- 시간표. 0~0.5초 확대 → 0.5~1.6초 물러남 → 1.8~3.2초 뚜껑 → 3.35~4.35초 카드 올라오고 봉투 내려감 → 4.45~5.35초 카드 확대와 봉투 퇴장. 5.4초쯤 끝난다.
- ⑤ 시작할 때 스크롤 복원을 끄고(history.scrollRestoration = manual) 맨 위로 옮긴다(behavior instant라 부드러운 스크롤 설정을 무시한다).
- ⑥ 오른쪽 아래에 "넘어가기"(고운바탕 15px, 잉크색, 얇은 밑줄. 넓은 화면에서는 페이지 기둥 안). 0.6초에 나타나 4.3초에 사라진다. 처음에 "Skip"을 필기체(나눔손글씨 펜)로 했다가 이산하가 문구와 글꼴을 Scene1의 고운바탕으로 바꿔 달라고 해서 바꿨다. 탭은 어디를 눌러도 건너뛴다.
- 확인. 조립본 파일과 localhost:3000에서 프레임 16장(0.15~5.9초)이 같고, 마지막 프레임과 커버 정지 화면의 픽셀 차 0(둘 다). tsc · eslint 통과.

### 2026-09-06 22:50:23 이전 · 이산하 (T38)

- 봉인은 넘어갔을 때 가려져야 하지 않나. 닫혀 있을 때는 뚜껑 위에 있지만 넘기면 뚜껑 뒤에 있게 되는 것 아닌가. 이것까지 구현하기 힘든가. 현실적으로 무리면 봉인을 띄운 뒤 봉투 오른쪽 적당한 자리에 두고, 편지지가 커지며 가려져 봉투와 함께 사라지게 하자.

### 2026-09-06 22:50:23 · Claude (T38)

- 맞는 지적이라 그대로 구현했다. 봉인은 뚜껑에 붙어 함께 기울어지다가 뚜껑이 옆면(90°)을 지나는 순간 바깥면 그림과 함께 숨는다. 기울어지는 동안 원근 때문에 점점 얇아져 옆면에서는 선 하나가 되므로 사라짐이 튀지 않는다. 오른쪽에 띄우는 대안은 쓰지 않았다.
- 돌아보면 T37 ②의 "갑자기 날아간다"는 뚜껑이 1.0초로 빨랐던 탓이고, 숨기는 것 자체는 맞았다. 그때 숨기지 않는 쪽으로 바꾼 것을 되돌린 셈이다.
- 면 교체 시각을 고쳤다. 뚜껑 곡선 cubic-bezier(0.55, 0, 0.3, 1)은 진행 50%가 시간 44.4%에 오므로 90°는 1.8 + 1.4 × 0.444 = 2.42초다. 교체가 2.5초여서 그 0.08초 동안 바깥면(봉인 포함)을 뒤에서 보고 있었다(2.48초 프레임에 봉인이 뚜껑 끝 위에 크게 보였다). 교체를 2.42초로 옮겼다.
- 확인. localhost:3000 프레임에서 봉인 색 픽셀 수가 2.30초 3036 → 2.38초 69(옆면 선 하나) → 2.41초부터 0이다. 마지막 프레임과 커버 정지 화면 픽셀 차 0. tsc · eslint 통과.

### 2026-09-06 22:56:03 이전 · 이산하 (T39)

- 봉인이 반쯤 넘어갔을 때(90° 이후) 아예 사라진다. 뒷면처럼 해서(봉인 뒷면은 에셋이 없으니 적당한 어두운 톤) 봉투 뚜껑이 봉인의 1/4쯤을 아래에서 피자 조각처럼 차지하는 느낌이어야 하지 않나. 왜 저렇게 됐나.
- (앞서) 편지지가 좀 더 빠르게 나올 수 있나, 봉투와 겹치지 않으면서. 가능한지부터 봐 달라. → (Claude의 A·B 제안에) B로 해 달라.

### 2026-09-06 22:56:03 · Claude (T39)

- 맞는 지적이다. 봉인(폭 90px)이 뚜껑 끝(봉인 가운데 높이에서 폭 56px)보다 커서, 젖혀진 뒤에는 뚜껑 끝 둘레로 봉인 뒷면이 삐져나와 보여야 한다. T38에서 90°에 봉인을 통째로 숨겨서 그 부분까지 없앴다.
- 뒷면 에셋 `wax-seal-back.svg`를 만들었다. 앞면과 같은 일그러뜨림이라 윤곽이 같고, 납작한 어두운 밀랍(#3B2411, 가운데 조금 밝게, 잔 결)이다. 뚜껑 안에서 안쪽면 *아래*에 같은 자리로 두고 90°(2.42초)에 앞면과 바꾼다. 젖혀진 뒤 뚜껑 끝이 봉인 뒷면의 가운데 아래를 피자 조각처럼 가리고 둘레가 보인다.
- 편지지 속도. 카드를 가리는 선이 매 프레임 봉투 윗변과 같아 속도와 무관하게 겹치지 않는다. A(뚜껑이 닿는 3.2초부터 0.6초) · B(뚜껑이 173°인 2.9초부터, 젖혀진 뚜껑은 카드 뒤)를 제안했고 이산하가 B를 골랐다. 카드·봉투 시간표는 1.5초(0.6초 올라옴 · 0.075초 멈춤 · 0.825초 확대)로 2.9초에 시작해 4.4초에 끝난다. 넘어가기는 3.6초부터 사라진다. 안전 타이머 6.5초.
- 확인. localhost:3000 프레임에서 2.5~2.9초에 봉인 뒷면이 뚜껑 끝 위로 보이고, 2.9초부터 카드가 올라오며 4.4초에 커버다. 마지막 프레임과 커버 정지 화면 픽셀 차 0. tsc · eslint 통과.
- 작업 트리에 Claude가 만들지 않은 파일(`docs/artifacts/assets/fleuron.svg` · `lace-frill.svg` · `oval-card.svg`, `public/lace/`)이 있다. 건드리지 않았고 커밋하지 않았다.

### 2026-09-06 23:08:04 이전 · 이산하 (T40)

- 지금은 봉투가 줌아웃된 뒤에 열리는데, 하나로 합치면 더 줄일 수 있겠다. 줌아웃이 시작될 때 봉투 열기도 같이 시작해 달라. 봉투가 80%쯤 열릴 때 편지지가 40% 빠져나오고, 그다음 봉투는 자연스럽게 fade out, 편지지는 전체 화면으로 확대. 어떤 것 같나, 불가능한 부분이 있나. → (Claude의 시간표 제안에) 이대로 만들자.

### 2026-09-06 23:08:04 · Claude (T40)

- 가능하다고 답했고, 한 군데 구조를 바꿨다. 지금까지는 카드의 봉투 안 부분을 clip-path로 가리며 그 선을 봉투 윗변과 맞추느라 카드와 봉투를 한 시간표에 묶어야 했다(T37 어색함의 뿌리). 이제 봉투 몸과 뚜껑을 카드보다 앞 층에 둔다. 바탕(z 25) < 카드 = 커버(z 30) < 봉투 몸(z 35) < 뚜껑(z 36). 봉투 안에 든 카드 부분은 봉투가 자연히 가리고, 봉투가 흐려지면 그 뒤의 카드가 그대로 드러난다. 뚜껑은 옆면을 지나는 0.92초에 z 28로 내려가 카드 뒤로 간다(옆면이라 눈에 안 띈다). 카드가 봉투 밑변 아래로 삐져나오는 부분만 클립으로 가리고, 그 선은 카드 올라옴·봉투 하강과 같은 시간표라 봉투 밑변을 정확히 따라가며 올라오기가 끝나면 사라진다.
- 시간표(2.6초). 0~0.3 확대된 채 → 0.3~0.9 줌아웃, 동시에 0.3~1.7 뚜껑(옆면 0.92) → 0.8~1.4 카드 올라오고 봉투 조금 내려감(뚜껑 80%인 1.13초에 카드 약 40%) → 1.4~1.8 봉투와 뚜껑이 흐려져 사라짐 → 1.8~2.6 카드가 가운데 기준으로 확대. 넘어가기는 0.4~1.9초. 안전 타이머 5초. 탭은 document에서 받는다(카드 위를 눌러도 건너뛰도록). 봉투 몸 층과 뚜껑 층은 같은 확대·하강·흐려짐 애니메이션을 써서 한 몸처럼 움직인다.
- 확인. 조립본 파일과 localhost:3000에서 프레임 14장(0.15~3.0초)을 찍었다. 1.15초에 뚜껑이 80%쯤 열리고 카드가 40%쯤 나와 있으며, 1.45초에 봉투가 반쯤 흐려져 뒤의 카드가 비친다. 두 곳의 차이는 캡처 시각 오차뿐이다. 마지막 프레임과 커버 정지 화면 픽셀 차 0(Next.js) · 3/255(조립본). tsc · eslint 통과.

### 2026-09-06 23:13:34 이전 · 이산하 (T41)

- 애니메이션이 너무 급하게 휘리릭 나오는 느낌이다. 방향은 마음에 드니 전체 시간을 적절히 늘려 보자. 봉투 대비 편지지 크기가 50%쯤으로 보이는데 92%로. 봉투가 흐려지는 대신 아래로 화면 끝까지 쭉 내려가며 사라지는 건 어떤가. 그리고 로딩이 끝나고 Scene1이 뜨는 시점에 왜 한 번 깜빡이는 느낌이 나나.

### 2026-09-06 23:13:34 · Claude (T41)

- 깜빡임의 원인. Next.js에서 장면이 끝날 때 커버의 층 지정(`is-intro`, z-index 30)을 animationend 처리에서 바로 지웠고, 봉투 층은 React가 다음 렌더에서 지웠다. 그 사이 한 프레임 동안 바탕(z 25)이 커버를 덮었다. 매 프레임 상태를 기록해 확인했다. 고치기 전에는 봉투 층이 있는데 커버 z-index가 auto인 프레임이 정확히 1개(3883ms), 고친 뒤에는 0개다. 층 지정 해제를 봉투 층이 빠지는 렌더와 같은 프레임(useLayoutEffect)으로 옮겼다. 조립본은 원래 한 작업에서 둘을 지워 문제가 없었다.
- 시간표를 2.6초 → 3.9초로. 0~0.4 확대된 채 → 0.4~1.2 줌아웃, 동시에 0.4~2.2 뚜껑(옆면 1.2) → 1.0~2.0 카드 올라오고 봉투 조금 내려감 → 2.1~2.8 봉투가 화면 아래로 빠져나감(ease-in) → 2.85~3.85 카드 확대. 넘어가기는 0.5~2.9초. 안전 타이머 6.5초.
- 카드 폭은 봉투의 60% → 92%. 올라온 뒤 65%가 나와 있고 화면 세로 가운데에 선다.
- 봉투는 흐려지지 않고 아래로 빠져나간다. 봉투 몸과 뚜껑 층이 같이 내려가고, 카드의 삐져나온 부분을 가리는 클립 선도 구간마다 같은 시간표·곡선으로 봉투 밑변을 따라간다. 뒤 구간 애니메이션은 forwards만 두어 앞 구간 값을 덮지 않는다.
- 확인. localhost:3000 프레임 13장(0.2~4.4초)과 조립본 파일. 마지막 프레임과 커버 정지 화면 픽셀 차 0(둘 다). tsc · eslint 통과.

### 2026-09-06 23:19:30 이전 · 이산하 (T42)

- (레퍼런스 사진: 봉투 입구에 카드가 끼워져 아래가 V자로 잘려 보이는 청첩장) 편지지가 아래만 삼각형이고 전체가 네모나지 않다. 이 레퍼런스처럼 진짜 편지봉투(편지지가 들어갈 곳이 있는 형태)로 수정할 수 있나. → 봉투 에셋은 셋이어야 할 것 같다. 뒷판(그냥 네모) · 앞판 · 뚜껑. 지금은 1+2가 하나고 3이 있다.

### 2026-09-06 23:19:30 · Claude (T42)

- 봉투를 세 장으로 나눴다. 뒷판 `envelope-back.svg`(그냥 네모. 속이라 조금 어둡고 위 접힌 곳 그늘이 짙다), 앞판 `envelope-front.svg`(위 꼭짓점에서 가운데로 내려오는 옆 날개 둘 + 아래 날개. 옆 날개 사이 위쪽 삼각형은 입구라 투명. 입구 쪽 가장자리에 잘린 종이의 빛 선과 그늘), 뚜껑은 그대로. 전의 뒷면 에셋(옆 날개가 바탕 전체를 덮어 입구가 없던 것)은 없앴다.
- 층. 바탕(z 25) < 뒷판(26) < 카드(30) < 앞판(35) < 뚜껑(36 → 옆면에서 28). 카드는 앞판 뒤, 뒷판 앞에 끼워져 입구로 보인다. 뚜껑이 들리면 입구로 카드 윗부분이 V자로 보이고, 올라온 뒤에도 아랫부분은 옆 날개 뒤에 있다. 레퍼런스와 같은 모양이다. 뒷판·앞판·뚜껑 층은 같은 확대·하강 애니메이션을 쓴다.
- 뒷판은 입구로만 보이므로 1.5배(149KB), 앞판은 확대 시작 장면에 보이므로 3배(543KB).
- 확인. localhost:3000 프레임 9장. 1.2초(뚜껑 옆면)에 입구로 카드 윗부분이 보이고, 1.8~2.1초에 카드 아랫부분이 옆 날개 뒤에 있다. 마지막 프레임과 커버 정지 화면 픽셀 차 0. tsc · eslint 통과.

### 2026-09-06 23:36:16 이전 · 이산하 (T43)

- ① 봉투 처음 크기가 너무 커서 줌아웃에 시간이 많이 든다. 크기를 지금의 2/3로 하고 높이 기준으로 맞추면 줄겠다. ② 전체적으로 줌아웃 → 편지 꺼내기가 급하게 휘리릭이다. 진짜 편지를 빼는 것처럼. ③ 봉투가 열리는 것과 편지지가 나오는 것이 겹치는 시간이 있다. 편지지 속도를 늦추든지 해서 맞춰야 한다. 종합하면, 줌아웃됐을 때 봉투 가로폭이 화면에 꽉 차고, 편지지가 나오면 이미 화면에 꽉 찬 상태가 되게 하고, 그 상태에서 봉투가 아래로 내려가면 화면이 열리듯 될 것 같다. 어떻게 생각하나(바로 하라는 게 아니라 느낌을 봐 달라). → (Claude의 숫자 제안에) 이 숫자대로 만들어 달라.

### 2026-09-06 23:36:16 · Claude (T43)

- 좋은 방향이라 답했고 숫자로 옮겼다. 편지지는 커버 자체를 1:1로 봉투에 끼운 것이라 확대 구간이 없어졌다. 봉투 폭을 화면 폭(페이지 폭 430까지)의 108%로 두면 편지지(봉투의 92%)가 정확히 화면 폭이다. 시작 배율은 봉투 높이 = 화면 높이의 2/3(줌아웃 비율 4배 → 2배).
- 시간표(4.3초). 0~0.4 확대된 채 → 0.4~1.4 줌아웃, 동시에 0.4~2.4 뚜껑(옆면 1.29초) → 뚜껑이 90%인 1.8초부터 3.2초까지 편지지가 올라와 화면 맨 위(최종 자리)에 닿음. 이때 봉투는 가만히 있다 → 0.1초 쉬고 3.3~4.3초 봉투가 아래로 빠져나감(가속). 편지지의 남은 부분이 봉투 밑변을 따라 드러나며 끝난다. 넘어가기는 0.5~3.8초. 안전 타이머 7초. 끝은 클립 애니메이션의 animationend.
- 카드의 둥근 모서리와 그림자는 없앴다. 최종 자리에서 화면 폭이라 보이지 않고, 화면 위 모서리가 둥글면 커버와 달라진다.
- 확인. localhost:3000 프레임 12장(0.2~4.6초)과 조립본 파일. 1.4초에 입구로 편지지 윗부분이 보이고, 3.0초에 편지지가 맨 위에 닿아 봉투가 아래 2/3를 덮고 있으며, 3.2~4.1초에 봉투가 내려가며 드러난다. 마지막 프레임과 커버 정지 화면 픽셀 차 0(둘 다). 끝날 때 바탕이 커버를 덮는 프레임 0. tsc · eslint 통과.

### 2026-09-06 23:45:31 이전 · 이산하 (T44)

- 속도 자체는 좋다. 그런데 편지지가 위로 나오는 것과 봉투가 아래로 내려가는 것이 동시에 되어야 할 것 같다. 편지지는 조금 나온 뒤에 전체 화면을 차지하러 나오고, 봉투는 약간만 내려가며 fade out 되게. → (조금 나온 뒤 멈칫하는 판을 보고) 편지지가 나온 뒤 한 번 멈칫하고 봉투가 내려가며 사라진다. 편지지는 위로 나오면서 자연스럽게 화면 전체를 차지하고, 봉투는 아래로 내려가면서 자연스럽게 사라지는 것이 끊김 없이 이어져야 한다. → 줌아웃됐을 때 가로폭이 전체를 차지하는데 92%쯤 차지하게 바꿀 수 있나.

### 2026-09-06 23:45:31 · Claude (T44)

- 셋을 한 동작으로 묶었다. 편지지는 1.8~3.6초에 한 번에 최종 자리까지 올라오고, 봉투는 편지지가 조금 나온 2.3초부터 70px 내려가며 2.6초부터 흐려져 3.6초에 함께 끝난다. 처음에 만든 "조금 나온 뒤 0.2초 멈춤" 판은 이산하가 끊긴다고 해서 없앴다.
- 봉투가 흐려지려면 카드의 봉투 밑변 아래 부분을 가리던 클립을 쓸 수 없다(흐려질 때 잘린 선이 남는다). 대신 앞판 층 안에 바탕색 "바닥"(`.intro__floor`, 봉투 밑변 아래 전부)을 두었다. 바닥은 봉투와 함께 내려가고 흐려지므로 카드 아랫부분이 봉투와 같은 박자로 드러난다. 커버에는 이제 transform만 남았다.
- 봉투 폭은 108% → 92%. 커버 1:1보다 좁아지므로 편지지는 봉투 안에서 봉투 폭의 92%(화면의 약 85%)로 있다가 올라오며 화면 폭이 된다(translateY + scale 한 키프레임). 앞판이 앞 층이라 확대가 섞여도 가릴 것이 없다.
- 곁가지. 바닥이 앞판 층(z 35)에 들어가면서 바탕 층(z 25)의 넘어가기 버튼을 덮었다. 넘어가기를 고정 위치 z 40으로 올렸다.
- 확인. localhost:3000 프레임 10장과 조립본 파일. 2.0~2.6초 편지지가 연속으로 올라오고, 2.9~3.3초 봉투가 내려가며 반투명해져 뒤의 편지지가 비치고, 3.5초에 화면 폭이다. 마지막 프레임과 커버 정지 화면 픽셀 차 0(조립본) · 2/255(Next.js). 끝날 때 바탕이 커버를 덮는 프레임 0. tsc · eslint 통과.

### 2026-09-06 23:47:37 이전 · 이산하 (T45)

- 편지지가 나오면서 확대되는데 봉투가 천천히 사라지니까 이상하다. 봉투가 열리고 나서 바로 fade out 하고 편지지가 확대되는 것은 어떤가.

### 2026-09-06 23:47:37 · Claude (T45)

- 그대로 했다. 뚜껑이 다 열린 2.4초에 봉투(뒷판·앞판·바닥·뚜껑)가 0.5초 동안 흐려져 사라지고, 그 끝자락인 2.7초부터 편지지가 1.2초 동안 올라오며 화면 폭으로 커져 최종 자리에 닿는다. 봉투 하강은 뺐다. 3.9초에 끝난다. 넘어가기는 0.5~3.1초.
- 확인. localhost:3000 프레임 10장과 조립본 파일. 2.5초에 봉투가 반쯤 흐려져 편지지 아랫부분이 비치고, 2.7초에 봉투가 거의 사라진 채 편지지가 올라오기 시작하며, 3.9초에 화면 폭이다. 마지막 프레임과 커버 정지 화면 픽셀 차 0(둘 다). 끝날 때 바탕이 커버를 덮는 프레임 0. tsc · eslint 통과.

### 2026-09-06 23:53:46 이전 · 이산하 (T46)

- 진심인가. 봉투에서 편지지가 나오는 느낌이 아니다. 봉투가 열리는 데까지는 좋다. 그다음 봉투가 사라지고(빠른 fade out) 편지지가 화면 전체로 확장되며 가득 채워 Scene1이 되면 된다. → 정확히는, 봉투가 80% 열린 상태부터 편지지가 좀 올라오고, 봉투가 사라진 뒤에 편지지가 전체 화면으로 확대되면 된다.

### 2026-09-06 23:53:46 · Claude (T46)

- 원인은 편지지 크기였다. 봉투 폭의 92%로 두면 편지지(세로 화면 비율)가 봉투 높이의 3배라서, 봉투가 사라진 뒤 윗변만 화면 가운데에 걸린 벽처럼 보이고 확장이 아니라 밀려 올라감이 됐다. 그 배율에서는 확대의 고정점이 화면 밖(약 2000px)이라 이동으로만 보인다. 편지지를 봉투 폭의 55%(높이는 봉투의 약 1.8배)로 줄여, 봉투가 사라지면 네 변이 다 보이는 작은 편지지가 남고 그것이 화면 전체로 커진다.
- 시간표(4.0초). 0~0.4 확대된 채 → 0.4~1.4 줌아웃, 동시에 0.4~2.4 뚜껑 → 뚜껑 80%인 1.58초부터 0.8초 동안 편지지가 제 높이의 40%만큼 올라옴 → 2.4~2.9 봉투가 흐려져 사라짐 → 2.9~4.0 편지지가 화면 전체로 커짐. 넘어가기는 0.5~3.1초.
- 확인. localhost:3000 프레임 10장과 조립본 파일. 1.8~2.4초에 편지지가 봉투 위로 나와 있고, 2.6초에 봉투가 흐려지며 편지지 전체가 드러나고, 2.9초에 작은 편지지만 남았다가 3.2~3.8초에 커진다. 마지막 프레임과 커버 정지 화면 픽셀 차 0(둘 다). 끝날 때 바탕이 커버를 덮는 프레임 0. tsc · eslint 통과.

### 2026-09-06 23:56:52 이전 · 이산하 (T47)

- 봉투 사라지는 것을 0.3초로 줄이고 뒤를 당기면 더 낫지 않을까. 그런데 편지지는 왜 다시 작아졌나.

### 2026-09-06 23:56:52 · Claude (T47)

- 흐려짐을 0.5초 → 0.3초(2.4~2.7)로 줄이고 확대를 2.9초 → 2.7초로 당겼다. 3.8초에 끝난다. 넘어가기는 0.5~3.0초.
- 편지지는 봉투 폭의 92%로 되돌렸다. T46에서 55%로 줄인 것은 잘못 짚은 것이었다. 벽처럼 보인 진짜 원인은 크기가 아니라, 편지지가 올라오기 전에 봉투가 사라져 아랫변이 화면 밖에 있던 것이다. 지금은 뚜껑 80%부터 편지지가 제 높이의 35%만큼 먼저 올라오므로 92%여도 봉투가 사라질 때 네 변이 다 화면 안에 있고(390×844에서 윗변 60px · 아랫변 775px), 확대의 고정점도 화면 안(약 390px)이라 확장으로 보인다.
- 확인. localhost:3000 프레임 10장과 조립본 파일. 2.4초에 편지지가 봉투 위로 올라와 있고, 2.55초에 봉투가 흐려지며 편지지 네 변이 보이고, 2.9~3.5초에 커진다. 마지막 프레임과 커버 정지 화면 픽셀 차 0(조립본) · 2/255(Next.js). 끝날 때 바탕이 커버를 덮는 프레임 0. tsc · eslint 통과.

### 2026-09-07 00:00:26 이전 · 이산하 (T48)

- 지금 다 괜찮은데, 봉투가 fade out 되는 시점을 봉투가 아래로 내려가는 것으로 바꿔 편지지가 거의 전부 보이게 하고, 편지지가 확대되면서 봉투가 fade out(0.3초) 되게 해 달라.

### 2026-09-07 00:00:26 · Claude (T48)

- 그대로 했다. 뚜껑이 다 열린 2.4초부터 0.6초 동안 봉투가 아래로 내려간다. 윗변이 편지지 아랫변 70px 위까지 와서 편지지가 약 90% 보인다(390×844에서 403px). 2.9초부터 편지지가 화면 전체로 커지고, 그 시작에 봉투가 0.3초 동안 흐려져 사라진다. 4.0초에 끝난다. 넘어가기는 0.5~3.2초.
- 바닥(봉투 밑변 아래를 가리는 바탕색 면)은 봉투와 함께 내려가므로 봉투 윗변을 따라 편지지가 드러나고, 흐려질 때 남은 아랫부분이 드러난다.
- 확인. localhost:3000 프레임 10장과 조립본 파일. 2.6~3.0초에 봉투가 내려가며 편지지가 거의 다 보이고, 3.1초에 봉투가 사라지고, 3.3~3.9초에 커진다. 마지막 프레임과 커버 정지 화면 픽셀 차 0(조립본) · 2/255(Next.js). 끝날 때 바탕이 커버를 덮는 프레임 0. tsc · eslint 통과.

### 2026-09-07 00:02:33 이전 · 이산하 (T49)

- 아니, 편지지가 올라가는 동시에 봉투는 아래로 내려가야지.

### 2026-09-07 00:02:33 · Claude (T49)

- 봉투 하강을 편지지가 올라오는 1.58초(뚜껑 80%)로 당겨 둘이 같은 0.9초 · 같은 곡선으로 움직인다. 2.48초에 편지지가 약 90% 나와 있고, 2.6초부터 편지지가 화면 전체로 커지며 그 시작에 봉투가 0.3초 동안 흐려져 사라진다. 3.7초에 끝난다. 넘어가기는 0.5~2.9초.
- 확인. localhost:3000 프레임 10장과 조립본 파일. 1.8~2.1초에 편지지가 올라오고 봉투가 내려가는 것이 한 프레임에 같이 보이고, 2.4초에 편지지가 거의 다 나와 있으며, 2.75초에 봉투가 사라지고 2.9~3.5초에 커진다. 마지막 프레임과 커버 정지 화면 픽셀 차 0(조립본) · 2/255(Next.js). 끝날 때 바탕이 커버를 덮는 프레임 0. tsc · eslint 통과.

### 2026-09-07 00:15:21 이전 · 이산하 (T50)

- Scene1. 표제(우리의 삶을 함께 써 주신 당신께) 글자 크기를 3/4로 줄이고, WEDDING INVITATION과 표제의 굵기를 키우고, 이름 크기를 2/3로, 이름 세 줄 사이 간격을 2/3으로 해 달라. 아래 세 줄은 배경 아랫변 기준 48px 위에 있게 유지해 달라(배경이 100vh가 아니면 하지 말고 말해 달라). 기기에 따라 아래 글이 두 사람 그림과 겹치는 것 같다.
- Scene2. 원래 넣었던 그림을 지우고, 다른 세션에서 만든 레이스 에셋(`public/lace/`, 사용법은 `preview.html`)으로 Scene2 문구를 넣어 달라. 배치(크기·굵기)는 지금 상태를 고수하지 말고 더 나은 쪽으로.
- Scene3. 캐릭터를 전달할 테니 바로 만들지 말고 어떻게 하는 것이 제일 좋을지 제안해 달라. 말풍선이 일반적이지만 더 나은 방법도 좋다. 전체 무드는 비슷하게 가져가고 싶다.

### 2026-09-07 00:15:21 · Claude (T50)

- Scene1. 배경은 커버 상자 자체다(`.cover-bg`가 `#cover`에 꽉 차고 `#cover`는 `min-height: 100dvh`, 내용이 넘치지 않아 곧 100dvh). 100vh가 아니라 100dvh이지만 화면 아랫변이 곧 배경 아랫변이므로 4번을 그대로 했다. 표제 24 → 18px · 700, WEDDING INVITATION 500 → 600, 이름 30 → 20px, 간격 10 → 7px, 아래 여백 56px + 안전 영역 → 48px(안전 영역을 더하지 않는다. 홈 표시줄이 있는 전체 화면에서는 글이 표시줄 위 14px에 온다). 겹침의 원인은 아래 여백에 안전 영역이 더해지고 이름 상자가 118px라 짧은 화면(375×667)에서 이름 윗변(493)이 두 사람 아랫변(520)보다 위에 있던 것이다. 지금은 이름 상자 85px, 윗변 534로 14px 떨어진다. 390×844 · 430×932에서도 확인했다.
- Scene2. 레이스 프릴 · 타원 카드 · 플러런 세 장(같은 840×1110 화폭)을 한 상자에 겹치고, 글은 안쪽 인쇄 테두리 안에 세로 가운데로 쌓았다. 배치는 `preview.html`의 이름 카드가 아니라 핵심 정보용으로 새로 잡았다. 플러런 / 2026년(13px, 자간) / 10월 9일 금요일(20px 700) / 오후 6시 30분(15px) / 오크색 짧은 선 / 더채플앳청담(18px 700) / 3층 커티지홀(14px) / 플러런. "2026년 10월 9일 금요일"은 한 줄이면 10.2em이라 타원 안쪽(가장 넓은 곳이 390px 화면에서 161px)에 15px로도 꽉 차서 연도를 작은 윗줄로 뺐다. 판은 좌우 12px 여백 안에서 폭을 다 쓰고 화면 높이에도 맞춘다. Next.js는 `/lace/`를 쓰고 `public/scene2/opened-paper.png`는 지웠다. 에셋 원본(`assets/lace-frill.svg` · `oval-card.svg` · `fleuron.svg`)과 생성기 `docs/scripts/gen-lace-frill.py`는 이산하가 만든 것을 그대로 커밋했다. `public/lace/preview.html`은 커밋하지 않고 두었다. `docs/design/scene2/`의 원본과 `docs/design/canvas/opened-paper.png`(동결한 캔버스가 참조)는 지우지 않았다.
- Scene3 제안(만들지 않았다). 말풍선 대신 "쪽지 + 걸친 캐릭터"를 권한다. 한 줄 인사는 작은 종이 쪽지에 쓰고, 화자 캐릭터가 쪽지의 위 모서리 하나에 다이컷 조각(종이 테두리 + 그늘)으로 겹친다. 신랑(수달) 쪽지는 오른쪽 정렬에 오른쪽 위 모서리, 신부(토끼) 쪽지는 왼쪽 정렬에 왼쪽 위 모서리다. 꼬리가 없어도 겹친 캐릭터가 화자를 말해 준다. 이유 셋. ① 와이어프레임의 층 규칙(배경 위 종이, 종이 위 글, 그 위 오브제 걸침)과 그대로 맞는다. ② Scene1(종이 무대 위 컷아웃)·Scene2(종이 위 레이스 카드)와 같은 "종이 겹치기" 문법이라 무드가 이어진다. ③ 4~8쪽의 긴 메모지와 같은 부품이라 한 번 만들면 다 쓴다. 말풍선은 긴 본문에서 채팅처럼 보인다. 쪽지는 SVG → PNG 질감(종이 결 + 살짝 기울임 ±1°)이고 글은 고운바탕 15~16px · 줄 간격 1.7 · 요체다. 블록 높이는 0.3화면(약 260px)으로 두어 다음 블록의 사진 종이가 아래에 걸쳐 보이게 한다. 움직임은 스크롤로 들어올 때 쪽지가 6px 내려앉으며 나타나는 정도(0.4초)만 두고 움직임 줄이기면 없앤다. 이산하가 고전적인 말풍선을 원하면 같은 부품에 종이 오린 꼬리를 붙이면 된다.
- 재료 규격(이산하가 줄 것). 투명 배경 PNG, 캐릭터 하나당 세로 1000px 이상(화면에서 90~140px로 쓴다). 흰 테두리와 그늘은 넣지 않은 깨끗한 컷아웃(테두리·그늘은 Claude가 같은 규칙으로 붙인다. 가장자리 색 번짐은 `unmatte.py`로 지운다). 포즈는 캐릭터마다 둘, 상반신 3/4 정면에 손 흔드는 것(인사)과 기본(이야기). 토끼는 오른쪽을, 수달은 왼쪽을 보게 그려 서로 마주 보게 한다(좌우 뒤집기는 최후 수단). 표정은 기본 · 놀람(설렘) · 웃음 셋이면 되고 몸은 같은 포즈여도 된다(6·7쪽 "표정이 달라진다", Q9). 화풍은 Scene1 두 사람 컷아웃과 어울리는 낮은 채도의 종이 톤이고, 팔레트는 종이(#FBF7EF) · 잉크(#2F2A25) · 오크(#B98E5E)에 캐릭터별 포인트색 하나다. 파일은 `docs/design/characters/{rabbit,otter}--{wave,talk}--{plain,surprised,smile}.png`로 받는다. 재료가 오기 전에 회색 실루엣을 넣은 자리 시안을 먼저 만들 수 있다.
- 확인. 조립본 파일과 localhost:3000을 375×667 · 390×844 · 430×932에서 찍어 봤다. `/lace/` 세 장 200. tsc · eslint 통과.

### 2026-09-07 00:33:39 이전 · 이산하 (T51)

- Scene2. 글씨 크기가 제각각이다. 줄은 "2026년 10월 9일" / "금요일 오후 6시 30분" / 한 칸 띄고 / "더채플앳청담 3층 커티지홀" / "강남구 선릉로 757"로 하고, 글자 크기는 두 가지로 통일해 달라. 맨 위 문양, 중간의 선, 아래 문양은 지워 달라.
- Scene3. 제안한 것을 예시로 만들어 달라. 캐릭터는 회색 원으로 표시해도 된다(어차피 대체한다).

### 2026-09-07 00:33:39 · Claude (T51)

- Scene2. 네 줄로 바꾸고 장식(플러런 둘·선)을 지웠다. 큰 글자는 날짜·시간 두 줄(4.6cqw, 700, 잉크색), 작은 글자는 장소·주소 두 줄(3.5cqw, 400)이고 사이에 작은 글자 한 줄 높이(3.5cqw)를 띄웠다. 크기를 판 폭(cqw) 기준으로 잡았다. 타원 안쪽 폭이 판 폭에 비례하므로 화면이 달라도 같은 여유가 남는다. 가장 긴 줄 "더채플앳청담 3층 커티지홀"(12em)이 가장 넓은 곳(판 폭의 44%)에 들어가야 해서 작은 글자의 상한이 3.5cqw다. 390×844에서 큰 글자 16.8px · 작은 글자 12.8px, 375×667에서 16.1 · 12.3px, 430×932에서 18.7 · 14.2px이고, 가장 긴 줄의 좌우 여유는 390에서 10px이다. 큰 글자를 장소 줄로 바꾸고 싶으면 상한이 13px대로 내려가서 큰 글자가 되지 못한다.
- Scene3 예시. 쪽지 두 장이다. 신랑 쪽지(폭 80%, 오른쪽 정렬, 0.6° 기울임) "안녕하세요. 10월의 신랑, 이산하", 신부 쪽지(왼쪽 정렬, -0.8°) "신부 송시야입니다! / 잠깐 저희에 대해 얘기해 드릴게요!". 고운바탕 15px · 줄 간격 1.7 · 안쪽 여백 좌우 16 · 위 22 · 아래 20. 화자는 회색 원(54px, 종이 테두리 4px + 그늘)이고 쪽지 위 모서리에 걸친다(위로 34px, 밖으로 12px). 종이 결은 새 에셋 `assets/note-paper.svg` → `public/paper/note.png`(720×480, 214KB, `object-fit: cover`)이고 그늘·기울기는 CSS다. 쪽지는 화면에 들어올 때 8px 내려앉으며 나타난다(0.45초, IntersectionObserver, 한 번). 블록 높이는 390×844에서 233px(약 0.28화면)이고, 걸쳐 보이는 것을 확인하려고 4쪽 자리표시(`#part1-groom`, 사진 종이 한 장에 "신랑 어릴 적 사진 · 4쪽 자리")를 두었다. 4쪽을 만들 때 걷어낸다. 작은 화면(375)에서 왼쪽 원이 첫 줄 글자에 4px 겹쳐 원을 34px 위로 올리고 위 여백을 2px 더했다.
- 확인. 조립본 파일과 localhost:3000을 375×667 · 390×844 · 430×932에서 찍어 봤다. 쪽지 등장(`is-in`) 둘 다 켜지고 불투명도 1. `/paper/note.png` 200. tsc · eslint 통과.

## 결정

행의 내용은 불변입니다. 상태 칸만 갱신할 수 있습니다.
결정이 뒤집히면 행을 고치지 않고 새 행으로 대체한 뒤, 옛 행 상태에 "폐기 — 결정 N으로 대체"를 적습니다.

| # | 결정 | 결정자 | 상태 |
| --- | --- | --- | --- |
| 1 | 청첩장은 URL `/` 하나의 세로 스크롤 페이지입니다. IA 단위는 블록이고, 블록마다 앵커 id를 두어 명세·시안·코드가 같은 이름으로 부릅니다. 갤러리 뷰어는 같은 URL 위의 오버레이입니다 | Claude 제안 | 확정 (2026-09-06 T6) |
| 2 | 화면 명세는 `docs/ontology/ia/`에 URL 1개당 파일 1개로 둡니다. `/`는 `index.md`, 라우트 표는 `README.md`입니다. 와이어프레임 PDF는 git 밖이므로 `index.md`가 git 정본입니다 | Claude 제안 | 잠정 |
| 3 | HTML 시안은 `docs/artifacts/`에 URL 경로를 미러해 둡니다(`/` = `index.html`). 이미지는 상대 경로로 참조합니다. 라운드 비교가 필요한 시점의 사본은 `docs/ontology/references/`에 동결합니다 | Claude 제안 | 확정 (2026-09-06 T6) |
| 4 | 시안 확인은 Claude Code Artifact로 합니다. 발행 직전 `docs/scripts/inline-artifact.mjs`가 이미지를 웹 크기로 줄여 data URI로 인라인한 사본을 만들고, 같은 Artifact에 재발행해 링크를 하나로 유지합니다 | Claude 제안 | 폐기 — 결정 11로 대체 |
| 5 | 시안 생산은 이산하가 Claude Design으로 합니다. Claude는 산출물을 `docs/artifacts/`에 합치고, 발행하고, 피드백과 결정을 이 논의록과 `ia/`에 기록합니다 | Claude 제안 | 폐기 — 결정 7로 대체 |
| 6 | 시안과 명세의 계좌번호·양가 부모님 성함은 자리표시로 둡니다. 실값의 반영 시점·방식은 Q10으로 따로 결정합니다 | Claude 제안 | 잠정 |
| 7 | 시안은 Claude가 `/design`(Claude Code에 번들된 Claude Design 캔버스)으로 만듭니다. 이산하와 와이어프레임을 한 쪽씩 논의하고, 확정된 쪽을 아트보드 `Main.dc.html` 아래에 이어 붙입니다. 전체를 한 번에 만들지 않고 마지막에 다듬습니다 | 이산하 | 폐기 — 결정 11로 대체 (한 쪽씩 논의하는 방식은 11이 잇습니다) |
| 8 | 캔버스 작업 파일(`*.dc.html`·`canvas.json`)은 `docs/artifacts/canvas/`에 두고 커밋합니다. 캔버스용 축소 이미지는 `docs/design/canvas/`(git 제외)에 두고 파일명으로 참조합니다. 조립된 발행 파일은 커밋하지 않습니다. 이산하가 캔버스에서 직접 고쳐 저장했으면 Claude는 다음 작업 전에 그 저장본을 읽어 와 작업 파일에 반영합니다 | Claude 제안 | 확정 (2026-09-06 T6. 이산하는 캔버스에서 직접 고치지 않겠다고 했으므로 읽어 오기 규칙은 예비) |
| 9 | 조립본 `docs/artifacts/index.html`(결정 3)은 쪽마다가 아니라 마지막 다듬기 단계에 `Main.dc.html`에서 한 번 옮겨 만듭니다. 결정 4의 인라인 발행은 그때 합니다. 그 전 확인은 캔버스 링크로 합니다 | Claude 제안 | 폐기 — 결정 10으로 대체 |
| 10 | 조립본 `docs/artifacts/index.html`(결정 3)은 Scene1부터 캔버스와 나란히 유지합니다. 캔버스 = 배치·간격 논의, 조립본 = 글꼴·스크롤 확인과 이식 원본. 이산하는 조립본을 브라우저에서 파일로 열어 글꼴을 확인하고, 폰 확인이 필요하면 결정 4대로 인라인 사본을 Artifact로 발행합니다. 글꼴 확인 패널은 확정 후 지웁니다 | 이산하 (HTML 병행), Claude (패널·자리) | 폐기 — 결정 11로 대체 (조립본이 유일한 시안 정본이 됩니다) |
| 11 | Artifact·캔버스 발행을 중단합니다. 시안은 조립본 `docs/artifacts/index.html`(CSS·마크업 정본)과 Next.js 로컬 적용(`src/app/`)으로 만들고, 와이어프레임을 한 쪽씩 이산하와 논의해 이어 붙입니다. 이산하는 로컬 `next dev`와 조립본 파일을 브라우저·폰에서 확인하고, Claude는 Playwright 프레임 캡처로 검수합니다. 빈티지 같은 질감 에셋은 CSS로 흉내 내지 않고 SVG → PNG(`docs/scripts/render-asset.mjs`)로 만듭니다. 기존 Artifact 링크 두 개는 2026-09-06 상태로 동결합니다 | 이산하 | 확정 (2026-09-06 T31) |

## 정본 반영 점검 (완결 전)

점검 대상 목록의 정본 = `docs/dashboard/README.md` §논의 "완결 전 정본 반영 점검 대상" (정책·DB·API·IA + open-questions 열린 액션·requirements·risks·glossary·artifacts). 같은 커밋에서 대조합니다. 다른 논의록의 결정을 뒤집었으면 그 표의 상태 칸에 "폐기 — {이 파일}:결정 N으로 대체"를 적습니다.

## 적재

이 논의에서 생성된 epic·task·bugfix ID를 분해 승인 후 적습니다.
